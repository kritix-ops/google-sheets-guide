import { FORM_RESPONSES } from "@/content/datasets/adtech";

# Forms → Sheets pipeline

A Google Form linked to a Sheet is one of the cheapest, most reliable data pipelines you can stand up. The team uses them for conference RSVPs, vendor intake, monthly campaign briefs, post-mortem surveys after a botched launch, anywhere you need a few non-technical people to contribute structured data without learning a CRM. The catch: the data lands raw. Mixed-case emails, trailing whitespace, duplicate submissions when someone hits "Edit response" then "Submit" again. The pipeline is the form. The discipline is the cleanup.

This lesson sits at the intersection of two patterns you've already met: lesson 2.1's raw-rectangle rule (Form Responses 1 is the canonical raw tab, never touch it) and lesson 2.14's cleanup recipes (the formulas you write on a Clean tab to normalize the raw input).

## The shape of a linked sheet

When you create a Form and pick "Send to Sheets", Sheets creates a new tab named `Form Responses 1` (or `Form Responses 2` if you've already used the first one). The columns are fixed by the API:

| Column | Source | Notes |
|---|---|---|
| `A` | Timestamp | Server time, ISO format; never trust the user's clock |
| `B` | Email Address | Only present if the form requires sign-in. Beware of case drift. |
| `C..H` | Form questions | One column per question, in form-order |

The Form owns the first row (headers). Editing the headers in Sheets does NOT rename the Form questions, so the next response writes the original column name back. Don't try to "tidy" the headers; do cleanup in a separate tab instead.

## The "Form Responses 1" tab is your raw rectangle

Treat the Form Responses tab the same way lesson 2.1 treated raw data tabs: **don't aggregate inside it, don't add columns to the right that the next response could overwrite**. The pattern:

1. `Form Responses 1`: append-only, owned by the form.
2. `Clean`: derived. Contains the formulas that normalize emails, dedupe, and aggregate.
3. `Dashboard`: consumes `Clean`, never the raw tab directly.

Adding helper columns to `Form Responses 1` *seems* fine at first. It breaks when the form gets a new question and Sheets inserts the column in the middle, shifting your helpers to the wrong cell references. Keep the raw tab pristine.

<Compare>
  <ComparePanel label="Broken: helpers on Form Responses 1" tone="negative">
    <pre className="font-mono text-xs"><code>{`Form Responses 1:
  A  B  C  D  E  F  G  H   I (helper: =LOWER(B2))

Form owner adds a new question.
Sheets inserts the new column at H.
Existing H shifts to I.
Helper formula at I now reads
=LOWER(B2) of the wrong column.`}</code></pre>
    <p className="mt-2 text-sm">Form schema is owned by the Form, not by Sheets. Inserted columns shift everything to their right. Helpers in the raw tab are time bombs.</p>
  </ComparePanel>
  <ComparePanel label="Working: helpers on a Clean tab" tone="positive">
    <pre className="font-mono text-xs"><code>{`Form Responses 1: A:H (raw only)
Clean:
  A: ='Form Responses 1'!A2
  B: =LOWER(TRIM('Form Responses 1'!B2))
  C: ='Form Responses 1'!C2
  ...

Form owner adds a new question.
Helpers on Clean keep working
because they reference by column name.`}</code></pre>
    <p className="mt-2 text-sm">Cleanup lives on its own tab. The raw tab stays untouched. Form schema changes touch the raw tab; helpers on Clean adapt explicitly.</p>
  </ComparePanel>
</Compare>

### Easy trap: the tab name has spaces, so it must be single-quoted

The tab Sheets creates is named `Form Responses 1` (three words, two spaces). Any cross-tab reference to a tab whose name contains spaces, punctuation, or starts with a digit must be wrapped in single quotes. Drop the quotes and Sheets parses `FormResponses1` as a name it doesn't recognize and returns `#ERROR!` or, worse, silently resolves it to a different range.

<Compare>
  <ComparePanel label="Broken: unquoted tab name with spaces" tone="negative">
    <pre className="font-mono text-xs"><code>{`=FormResponses1!A2
=Form Responses 1!A2

Sheets can't parse either form.
First: no tab named "FormResponses1".
Second: spaces break the reference
right after "Form".

Result: #ERROR! or #REF!`}</code></pre>
    <p className="mt-2 text-sm">A tab name with spaces is not a valid bare identifier. Sheets needs the quotes to know where the name ends and the `!` begins.</p>
  </ComparePanel>
  <ComparePanel label="Working: single-quoted tab name" tone="positive">
    <pre className="font-mono text-xs"><code>{`='Form Responses 1'!A2
=LOWER(TRIM('Form Responses 1'!B2))

Single quotes wrap the entire tab name.
The ! separates the tab from the cell.
Sheets resolves cleanly.`}</code></pre>
    <p className="mt-2 text-sm">Single quotes (not double, not backticks) wrap any tab name that isn't a bare alphanumeric word. The rule applies to every cross-tab reference: `IMPORTRANGE`, `QUERY`, `VLOOKUP` lookup ranges, charts pointing at a named range, all of it.</p>
  </ComparePanel>
</Compare>

<QuickCheck
  question="Two weeks after you launched a Form, the form owner adds a new question to the middle of the form. What happens to the Sheet?"
  options={[
    {
      id: "a",
      label: "A new column is appended to the right; existing columns are unchanged",
      correct: false,
      explanation: "If the new question is added to the middle, Sheets inserts the column in the middle to match the form-order, not at the right. The existing columns past the insertion shift one to the right.",
    },
    {
      id: "b",
      label: "A new column is inserted in the middle of Form Responses 1 to match the form's question order, shifting later columns right",
      correct: true,
      explanation: "The form owns the schema. Sheets mirrors it. Mid-form insertion means a mid-sheet column insertion. Anything that referenced columns past the insertion (helpers, charts, downstream formulas) is now pointing at the wrong column.",
    },
    {
      id: "c",
      label: "Sheets pops up a 'schema changed' dialog asking how to migrate",
      correct: false,
      explanation: "Sheets does not warn. The column appears silently. Audit downstream references after every form change.",
    },
  ]}
/>

## The five cleanup recipes you'll use most

```
=COUNTA(B2:B21)                                      raw response count
=COUNTA(UNIQUE(LOWER(TRIM(B2:B21))))                 distinct people
=COUNTIF(G2:G21,"Yes")                               Yes/No counts
=COUNTIFS(D2:D21,"Native EMEA",G2:G21,"Yes")          team × Yes/No breakdown
=SORTN(UNIQUE(LOWER(B2:B21)),5,0,1,1)                 5 most recent distinct emails
```

Three of these you'll write in the assignment. Two patterns to internalize:

**Always wrap email columns in `LOWER(TRIM(...))`** before passing them to `UNIQUE`, `COUNTIF`, or any matching function. The team has been bitten by `dina.dayan@flexelent.com` and `DINA.DAYAN@flexelent.com` counted as two people enough times. The cost of the wrapper is zero. The cost of the bug is a phone call from your manager asking why the rsvp count is off.

**Use `COUNTIFS` (plural) the moment you have two criteria**. The singular `COUNTIF` only takes one. Forms always have at least two breakdown axes (team and yes/no, or vertical and country), so you'll graduate to `COUNTIFS` within ten minutes of opening any real form analysis.

<Compare>
  <ComparePanel label="Broken: raw UNIQUE on emails" tone="negative">
    <pre className="font-mono text-xs"><code>{`Raw column B (case as submitted):
  dina.dayan@flexelent.com
  DINA.DAYAN@flexelent.com
  Dina.Dayan@flexelent.com

=COUNTA(UNIQUE(B2:B21))
➜ 3 (one person counted three times)`}</code></pre>
    <p className="mt-2 text-sm">Forms collect email address as the user submitted it. UNIQUE treats different cases as different values. Headcount estimates explode.</p>
  </ComparePanel>
  <ComparePanel label="Working: LOWER+TRIM first" tone="positive">
    <pre className="font-mono text-xs"><code>{`=COUNTA(UNIQUE(LOWER(TRIM(B2:B21))))
➜ 1 (normalized to dina.dayan@flexelent.com)

The wrapper costs nothing and dedupes
exactly the way you wanted.`}</code></pre>
    <p className="mt-2 text-sm">LOWER + TRIM is the canonical pre-UNIQUE pair for any user-submitted text. Track 1 lesson 9 covers text functions in depth.</p>
  </ComparePanel>
</Compare>

## Handling duplicates

Forms allow editors to edit responses (if you enabled it). Sheets logs the edit as a new row, leaving the original. You'll see two rows from the same person, same email, different timestamps. The cleanup pattern: keep the *latest* row per email.

```
=ARRAYFORMULA(
  IF(B2:B21="", , 
    IF(COUNTIFS(B2:B$21, B2:B21, A2:A$21, ">"&A2:A21)=0, "latest", "older")
  )
)
```

The formula counts how many later rows share this email. Zero later rows means this is the latest, so tag it `"latest"`. Add an extra column filter (or use `FILTER(... , latest_col="latest")`) and the duplicates fall out. The `ARRAYFORMULA` wrapper (lesson 1.10) is what lets the rule spill down every row of the response set instead of being copy-pasted per cell.

For the lesson assignment we keep it simpler: cleanup as count-distinct. Real workbooks layer the dedupe pattern on top of that.

<QuickCheck
  question="A teammate submits the form twice (they realized they forgot a field). Both rows have the same email; second has a later timestamp. You want to count each person once. Which formula gives the right answer?"
  options={[
    {
      id: "a",
      label: "=COUNTA(B2:B21)",
      correct: false,
      explanation: "COUNTA counts both rows. Returns the raw response count, not the distinct people count.",
    },
    {
      id: "b",
      label: "=COUNTA(UNIQUE(LOWER(TRIM(B2:B21))))",
      correct: true,
      explanation: "LOWER+TRIM normalizes case and whitespace. UNIQUE collapses duplicate emails. COUNTA returns the distinct count. This is the canonical cleanup pattern.",
    },
    {
      id: "c",
      label: "=COUNTIF(B2:B21, \"<>\")",
      correct: false,
      explanation: "COUNTIF with '<>' counts non-empty cells, same as COUNTA. Doesn't dedupe.",
    },
  ]}
/>

## Beyond the basics: Form features that show up in your Sheet

The Form Responses tab handles more than free-text and multiple-choice answers. Four features change the shape (or content) of the columns you'll be cleaning, and all four show up in the team's actual forms.

- **File upload questions.** A "Upload your invoice" question stores each upload as a Drive URL in the Sheet, one URL per submitter, in the question's column. The Drive file lives in a folder Forms auto-creates next to the form. The cleanup gotcha: the URL is a Drive link, not a file chip (lesson 2.5), so the `Aggregator` tab has to either accept it as a hyperlink string or convert it at read time. Vendor intake forms use file-upload questions for W-9s and signed contracts.
- **Section logic and branching.** Forms with `Go to section based on answer` (e.g., "Were you on the team that ran this campaign? If yes → section 2, if no → submit") create sparse rows in the Sheet: a respondent who skipped section 2 has blanks across those columns. `COUNTA` over a skipped column under-counts; design downstream formulas to expect blanks, and prefer `COUNTIFS` over a stable column (like the email or team field) that every branch fills.
- **Quiz mode → Sheets.** If the Form is set to `Quiz`, every response row gets an extra `Score` column at the right of the Sheet, plus a per-question `Points` column for each question that has a correct answer set. The Score column is the total points awarded; partial credit shows up as fractions of the per-question points. Training quizzes for new buyers use this to track who passed the platform-policy refresher.
- **Response notifications.** In the Form's Responses tab, the three-dot menu has `Get email notifications for new responses`. Turn it on and the form owner gets an email-on-submit for every response. Useful for low-volume forms (vendor intake, post-mortem requests) where you want to know within minutes; noisy for any form with more than a handful of responses a day. Pair this with a Sheet-side Apps Script trigger (Track 3 lesson 8) when you need richer alerts than "a new response arrived".

## Now do it for real

<MiniGrid
  data={FORM_RESPONSES.slice(0, 6)}
  caption="Form Responses 1 has 20 rows. The summary block lives in column J on the same tab."
/>

Click **Start lesson** to provision a fresh copy of the Form Responses tab. The raw data sits in `A1:H21`, with labels seeded in column I. Fill in column J:

- **`J1`**: total raw responses (`COUNTA`).
- **`J2`**: distinct people, with `LOWER + TRIM + UNIQUE`.
- **`J3`**: newsletter Yes count.
- **`J4`**: count of submissions from the `Native EMEA` team.

The grader checks each formula's shape AND its computed value, so swapping `COUNTIF` for `SUMPRODUCT` is fine, but skipping `LOWER` on the email column is not.

## Used in the wild

In the team's actual workbooks:

- **Conference RSVP forms feed the headcount estimate.** A single sheet pulls the linked form tab, dedupes by email, segments by team, and writes the per-team headcount into a budget dashboard. End-to-end: form → cleaned tab → dashboard, all in one workbook.
- **Vendor intake forms drive payment workflow.** The cleanup adds a `processed_at` column. Once a row is marked processed, accounting filters it out of the to-do view.
- **Monthly campaign brief forms feed the brief calendar.** Each brief becomes a row; the cleaned tab generates a `=HYPERLINK(...)` per row that links into the matching planning document. A people-chip column attaches the campaign owner.

## Pro pitfalls

- **Form question renames don't rename the column.** Editing the question in the Form leaves the existing column header alone in Sheets (so old responses stay queryable), but new responses still write to the same column. Don't rely on header text for `QUERY` matching; reference by column letter.
- **`Edit response` is enabled by default.** Most teams want it off. Otherwise editing creates duplicate rows that look intentional but aren't.
- **The email column is missing when sign-in isn't required.** A form without authenticated email will collect responses but column B will be blank. The form owner needs to flip "Collect email addresses" to on in the Form settings, not in Sheets.
- **`UNIQUE` on a column with mixed-case text returns BOTH cases.** Always wrap in `LOWER` (or `UPPER`/`PROPER`) before deduping email and name columns.
- **Forms don't enforce data types on free-text fields.** Numeric questions stored as "two hundred" (someone typed the word) break SUMs downstream. Either use a numeric question type in the form, or validate in the cleaned tab with `IFERROR(VALUE(F2), 0)`.
