import { CAMPAIGNS_LARGE } from "@/content/datasets/adtech";

# Pivot tables: rows, columns, values, calculated fields

A pivot table takes a long, narrow log and turns it into a short, wide summary. Same data, different shape. Once you can read a pivot, half the dashboard work in adtech is "what's the right pivot for this question". This lesson teaches the four slots in the Pivot editor (Rows, Columns, Values, Filters) and the calculated-field escape hatch when the four slots aren't enough.

For the first time in Track 2, you're meeting a feature that does the work of dozens of formulas. A pivot is what replaces a wall of `SUMIFS`, `COUNTIFS`, and `AVERAGEIFS` when the question is "break this number down by buyer and platform". The trade-off is interactivity: a pivot is configured in a dialog, not in a formula, so you can't templatize one with the same ease. The win is shape: one pivot delivers a 2-D matrix that would take 50 formulas to assemble by hand.

## The mental model

A pivot has one source range and four configuration slots. The source is always a rectangle of raw rows with a single header row at the top (lesson 2.1's "raw rectangle" pattern is what feeds pivots cleanly).

| Slot | What it does | Example |
|---|---|---|
| **Rows** | Group rows; one resulting row per distinct value | Buyer name |
| **Columns** | Group columns; one resulting column per distinct value | Platform |
| **Values** | Aggregate inside each Row × Column cell | SUM of Revenue |
| **Filters** | Discard source rows before aggregation runs | Country = "DE" |

The pivot's output is a Rows × Columns grid with the Values aggregated inside each cell. With Buyer on Rows, Platform on Columns, and SUM(Revenue) as the value, you get a buyer-by-platform revenue matrix. That's the single most common shape in adtech dashboards.

<MiniGrid
  data={[
    ["Buyer", "Google", "Taboola", "Outbrain", "MediaGo", "TikTok"],
    ["Yoav Cohen", 1099.30, "", 401.90, "", ""],
    ["Dina Dayan", 412.15, 198.40, "", "", ""],
    ["Maya Bar", "", "", 1124.60, 461.20, ""],
    ["Gal Vered", "", "", "", "", 322.05],
  ]}
  caption="The shape of a Buyer × Platform pivot with SUM(Revenue) as the value. Empty cells mean no campaigns of that buyer/platform combination."
/>

<QuickCheck
  question="You want to answer 'How does spend split between countries for each buyer this quarter?'. What goes in each slot?"
  options={[
    {
      id: "a",
      label: "Rows: Buyer, Columns: Country, Values: SUM Spend, Filters: Date in Q2",
      correct: true,
      explanation: "Buyer on Rows gives one row per buyer. Country on Columns gives one column per country. SUM Spend aggregates the spend per buyer-country cell. The Date filter scopes the source to Q2 before aggregation.",
    },
    {
      id: "b",
      label: "Rows: Country, Columns: Spend, Values: Buyer, Filters: Date in Q2",
      correct: false,
      explanation: "Spend is a numeric metric, not a category. It belongs in Values, not Columns. Buyer is a category, not a metric. Slot mismatch.",
    },
    {
      id: "c",
      label: "Rows: Buyer, Columns: Country, Values: Spend (no aggregation), Filters: Date in Q2",
      correct: false,
      explanation: "Values always carries an aggregation. 'Spend' alone is ambiguous: SUM, AVERAGE, MAX? The Pivot editor forces you to pick.",
    },
  ]}
/>

## Pivots vs a wall of formulas

The framing claim, "one pivot delivers a 2-D matrix that would take 50 formulas to assemble by hand", is easy to wave away until you've built that 2-D matrix the long way. Here is the buyer-by-platform revenue matrix done both ways on the same 60-row log.

<Compare>
  <ComparePanel label="The long way: a wall of SUMIFS" tone="negative">
    <pre className="font-mono text-xs"><code>{`Hand-maintained labels:
  J2:J6  = 5 buyer names
  K1:Q1  = 7 platform names

One formula, copied 5 x 7 = 35 times:
  K2 = SUMIFS($G$2:$G$61, $B$2:$B$61, $J2,
              $D$2:$D$61, K$1)`}</code></pre>
    <p className="mt-2 text-sm">Thirty-five near-identical formulas. New buyer joins the team and the dashboard does not update until you extend the labels and copy the row down. A buyer's name changes and you re-edit every row. Each <code>SUMIFS</code> re-walks the entire 60-row log on every recalc (Track 4 lesson 2 covers the cost when this is scaled to 50,000 rows).</p>
  </ComparePanel>
  <ComparePanel label="The pivot way: one definition" tone="positive">
    <pre className="font-mono text-xs"><code>{`Insert -> Pivot table
  Source: Campaigns!A1:G61
  Anchor: K1
  Rows:    Buyer
  Columns: Platform
  Values:  SUM Revenue`}</code></pre>
    <p className="mt-2 text-sm">One config block. New buyer arrives in the raw log: the pivot grows a row automatically. New platform appears: pivot grows a column. Same exact matrix, one configuration object instead of 35 formulas, and Sheets aggregates the source once instead of 35 times.</p>
  </ComparePanel>
</Compare>

## Anchoring a pivot

`Insert → Pivot table` opens the dialog. Pick "Existing sheet" and choose an empty cell (we use K1 in this lesson) so the pivot sits next to the raw log without overwriting it. The cell you choose becomes the **anchor** (the top-left of the pivot) and the pivot grows down and right from there.

The pivot anchor is special: the cell itself contains the pivot definition (not a value). Reading K1 from a script returns the pivot config, not the displayed number. The other cells the pivot fills (K2, K3, L1, L2, ...) hold the calculated `effectiveValue`s you can read like any normal cell.

<Compare>
  <ComparePanel label="Broken: anchored on top of data" tone="negative">
    <pre className="font-mono text-xs"><code>{`Source: A1:G61
Anchor: A1

Result: pivot overwrites the raw rectangle.
SUMIFS that read A:G break. Charts pointing at
A1:G61 now point at pivot output cells.`}</code></pre>
    <p className="mt-2 text-sm">The pivot grows down and right from its anchor. Anchoring inside the source range destroys the source.</p>
  </ComparePanel>
  <ComparePanel label="Working: anchored beside the data" tone="positive">
    <pre className="font-mono text-xs"><code>{`Source: A1:G61
Anchor: K1   (with H/I/J as a buffer)

Result: pivot fills K1 down and right.
Raw rectangle is untouched. Adding rows to
the source extends the rectangle, not the pivot.`}</code></pre>
    <p className="mt-2 text-sm">Always anchor a pivot in empty space at least one column away from the source. The buffer column also visually separates "raw" from "summary" as in lesson 2.1.</p>
  </ComparePanel>
</Compare>

## Summarize functions

The Values slot doesn't just take a column, it takes a column **plus** a function. These are the same aggregations from lesson 1.6 (`SUM`, `AVERAGE`, `MEDIAN`, `COUNTA`, `MIN`, `MAX`), now applied inside each pivot cell instead of inside a formula bar. The function determines what each cell shows. The headline options:

| Function | What it does | When to reach for it |
|---|---|---|
| `SUM` | Add up the values | Total spend, total revenue, total clicks |
| `COUNTA` | Count non-empty cells | Number of campaigns per buyer |
| `COUNTUNIQUE` | Count distinct values | Number of distinct verticals per platform |
| `AVERAGE` | Mean | Average CPC across a buyer's runs |
| `MIN` / `MAX` | Smallest / largest value | Worst-day spend, best-day revenue |
| `MEDIAN` | Middle value | Robust "typical" revenue (ignores outliers, unlike average) |

The pro move: **`MEDIAN` over `AVERAGE` for any metric that has long-tail outliers**. One blockbuster day pulls the average up; the median doesn't move. ROI distributions especially are skewed enough that the average misleads.

<Compare>
  <ComparePanel label="AVERAGE: skewed by outliers" tone="negative">
    <pre className="font-mono text-xs"><code>{`Buyer revenue per campaign:
[80, 95, 110, 88, 102, 4200]
AVERAGE ➜ 779.17`}</code></pre>
    <p className="mt-2 text-sm">The one blockbuster (4200) pulls the average up by 7×. Reported as "typical revenue per campaign", it's misleading. Most of the buyer's campaigns are around 100.</p>
  </ComparePanel>
  <ComparePanel label="MEDIAN: outlier-resistant" tone="positive">
    <pre className="font-mono text-xs"><code>{`Same six values:
[80, 88, 95, 102, 110, 4200]
MEDIAN ➜ 98.5`}</code></pre>
    <p className="mt-2 text-sm">The middle value (between 95 and 102) is unaffected by the 4200 outlier. Matches the eye's read of "typical": around 100. Use this for any metric whose distribution has a long tail.</p>
  </ComparePanel>
</Compare>

<QuickCheck
  question="Your pivot Values slot is set to AVERAGE of ROI. One blockbuster day with 9× ROI lifts the displayed average for the buyer above 2.5×, but their daily ROI median is 0.8×. Which aggregation tells the truer story for a 'typical day' dashboard?"
  options={[
    {
      id: "a",
      label: "AVERAGE: it includes every data point, so it's the most complete picture",
      correct: false,
      explanation: "AVERAGE includes every point but weights them equally. One outlier with extreme magnitude dominates. 'Most complete' is not 'most representative'.",
    },
    {
      id: "b",
      label: "MEDIAN: it returns the middle value, which is more representative when the distribution has outliers",
      correct: true,
      explanation: "MEDIAN is the right default for any skewed distribution. The blockbuster day is real but rare; the median 0.8× is what 'a typical day' actually looks like.",
    },
    {
      id: "c",
      label: "MAX: it shows the best the buyer can do, which is what matters for planning",
      correct: false,
      explanation: "MAX answers 'what's the best they've ever done', not 'what's typical'. Different question. Use MAX for outlier hunting, not for a 'typical day' headline.",
    },
  ]}
/>

## Calculated fields: the escape hatch

When a Values slot needs a ratio of two source columns (ROI, ROAS, CPC, margin), the four standard slots don't cover it. Calculated fields do. In the Pivot editor, click `Values → Add → Calculated field`, and write a formula like:

```
= 'Revenue' / 'Spend'
```

Names in single quotes reference source column headers (case-sensitive). The formula is evaluated **inside** the pivot's aggregation, so a calculated field for `'Revenue' / 'Spend'` divides the **summed** revenue by the **summed** spend per cell, which is what you want for a portfolio ROI per buyer-platform combo, not the average of per-row ROIs (which would skew toward small-spend rows).

The gotcha: calculated fields use SUM by default. If you need a different aggregation inside the calculated field, write it explicitly: `=SUMIF('Country','DE','Revenue') / SUMIF('Country','DE','Spend')`. Most of the time, the default SUM-of-the-formula is what you want, and you can leave the formula simple.

<Compare>
  <ComparePanel label="Broken: case mismatch" tone="negative">
    <pre className="font-mono text-xs"><code>{`= 'revenue' / 'spend'`}</code></pre>
    <p className="mt-2 text-sm">The source headers are <code>Revenue</code> and <code>Spend</code> (title case). Lowercase references return <code>#REF!</code> in the pivot output cells. Silently. No banner explains it.</p>
  </ComparePanel>
  <ComparePanel label="Working: exact match" tone="positive">
    <pre className="font-mono text-xs"><code>{`= 'Revenue' / 'Spend'`}</code></pre>
    <p className="mt-2 text-sm">Header text matches exactly. Calculated field evaluates as <code>SUM(Revenue) / SUM(Spend)</code> per pivot cell, the portfolio ROI you actually want.</p>
  </ComparePanel>
</Compare>

<QuickCheck
  question="You add a calculated field 'ROI' = 'Revenue' / 'Spend' to a buyer-by-platform pivot. Across all of Yoav's campaigns on Google, total spend is $2000 and total revenue is $3000. What does the Yoav/Google cell show?"
  options={[
    {
      id: "a",
      label: "1.5 (the average of all per-row ROI values on Yoav/Google)",
      correct: false,
      explanation: "Calculated fields don't average row-level ratios. They compute SUM(Revenue) / SUM(Spend) per cell. The cell value is SUM(3000) / SUM(2000) = 1.5, which happens to match the per-row average here only by coincidence.",
    },
    {
      id: "b",
      label: "1.5 (SUM of revenue divided by SUM of spend across Yoav's Google rows)",
      correct: true,
      explanation: "Right value, right reason. Calculated fields with the default behavior do SUM(numerator) / SUM(denominator), which is the portfolio ratio. 3000 / 2000 = 1.5.",
    },
    {
      id: "c",
      label: "The cell errors with #DIV/0! because you can't divide aggregates",
      correct: false,
      explanation: "You absolutely can divide aggregates. That's what calculated fields are built for.",
    },
  ]}
/>

## Filters: scope before aggregate

The Filters slot does what `WHERE` does in `QUERY` (lesson 1.12) or what a criterion does in `SUMIFS`: it drops source rows before the aggregation runs. With a filter in play, the pivot summarizes a slice of the log, not the whole log. Filters compose: a pivot can carry any number of them and they AND together.

Two filter patterns the team uses constantly:

- **Date window**: `Date is in the last 7 days` for a rolling weekly view that updates automatically as new rows arrive in the raw log.
- **Categorical exclusion**: `Buyer is not Maya Bar` while Maya is on parental leave, so her carryover campaigns don't distort the team's pivot.

<Compare>
  <ComparePanel label="Same source, different scopes" tone="positive">
    <pre className="font-mono text-xs"><code>{`Source: Campaigns!A1:G61 (60 rows)

No filter            -> pivot summarizes all 60 rows
Country = "DE"       -> pivot summarizes DE rows only
Country = "DE"  AND  -> pivot summarizes DE rows from
Date >= TODAY()-7       the last 7 days only`}</code></pre>
    <p className="mt-2 text-sm">Each filter is either a categorical "Filter by values" list or a "Filter by condition" rule. Filters apply to source rows, not to result cells, so a filter on Country still leaves Country available for use as a Row or Column slot.</p>
  </ComparePanel>
</Compare>

The trap: **a filter value that doesn't appear in the current source range silently produces an empty pivot**. No error, no banner, just an empty grid where the matrix used to be. The fastest debug is to clear the filter and confirm the pivot has rows, then add filters back one by one until the empty one is identified.

<QuickCheck
  question="You set the pivot Filters slot to 'Country is DE' but the pivot output is now empty. The raw log still has 60 rows. What's the most likely cause?"
  options={[
    {
      id: "a",
      label: "The pivot is broken; recreate it from scratch",
      correct: false,
      explanation: "Recreating doesn't diagnose. The pivot mechanism is fine; the issue is either in the filter or in the source's contents.",
    },
    {
      id: "b",
      label: "The current source range has no rows where Country = 'DE'",
      correct: true,
      explanation: "Filters drop rows before aggregation. A filter on a value that's not in the slice returns an empty result. Clear the filter; if rows reappear, the filter value was the problem.",
    },
    {
      id: "c",
      label: "DE is a two-letter code and pivots only accept three-letter ISO codes",
      correct: false,
      explanation: "Pivot filters accept any text. The filter compares values literally against whatever is in the source column.",
    },
  ]}
/>

## Now do it for real

<MiniGrid
  data={CAMPAIGNS_LARGE.slice(0, 6)}
  caption="60 rows of Campaign data fill A1:G61. The pivot will sit at K1, with empty H/I/J columns separating it from the raw log."
/>

Click **Start lesson** to provision the campaign log. Then anchor a pivot table at `K1`:

- **Source range**: `Campaigns!A1:G61` (the bounded raw rectangle).
- **Rows**: Buyer.
- **Columns**: Platform.
- **Values**: SUM of Revenue.

The result is the buyer-by-platform revenue matrix every team dashboard opens with. The grader reads the pivot definition back via the Sheets API and verifies each slot is configured correctly. Layout (horizontal vs vertical) and calculated fields land in lesson 2.3.

## Used in the wild

In the team's actual workbooks:

- **The opening pivot on every weekly review**: Rows = Buyer, Columns = Platform, Values = SUM Revenue. One look tells you who's running where and at what scale.
- **The vertical-mix pivot**: Rows = Vertical, Values = SUM Spend, COUNTA Campaigns, AVERAGE ROI. Three values stacked in one pivot. Tells you "where's the money going, and is it working".
- **The country pivot for budgeting**: Rows = Country, Columns = Quarter (a calculated field grouping the Date column), Values = SUM Spend. Quarterly geo allocation in one shape.

## Pro pitfalls

- **Pivot source pointing at the full column** (`Campaigns!A:G`). The pivot recomputes against a million empty rows on every edit. Bound the source: `A1:G61`, `A1:G500`, whatever fits the actual data. Same recalc cost as the bounded-vs-unbounded discipline from lesson 2.1, applied to pivots instead of formulas.
- **Editing the raw log under a live pivot** rearranges the pivot's groupings. New buyer names appear as new rows; new platforms appear as new columns. That's usually good, but if the pivot is the basis for a stable downstream report, freeze the source range or copy the pivot's output to a static range.
- **Calculated fields that reference a column header containing a space** must use the exact spelling and casing. `'Total Spend'` is different from `'Total spend'`.
- **`AVERAGE` of a ratio is not the ratio of averages**. Calculated `'Revenue'/'Spend'` returns SUM(Revenue) / SUM(Spend) per cell, which is the portfolio ratio. If you want the average of per-row ratios, compute the ratio in a source column first, then SUM or AVERAGE it.
- **Pivots don't survive copy-paste cleanly**. Copying a range that contains pivot output cells pastes static values; the pivot doesn't re-anchor at the destination. To duplicate a pivot, re-create it from the source.
- **`IMPORTRANGE`-fed sources expand without warning** (`IMPORTRANGE` mechanics covered in lesson 1.27). When the upstream sheet adds rows, the pivot's source range may not extend if you bounded it. Either set the source generously (`A1:G5000`) or accept that the pivot's "data area" needs re-checking after every upstream schema change.
