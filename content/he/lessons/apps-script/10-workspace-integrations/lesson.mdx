import { CAMPAIGNS_LARGE } from "@/content/datasets/adtech";

# אינטגרציות Workspace: Drive, Calendar, Gmail

bound Apps Script project לא רואה רק את ה-spreadsheet שהוא חי בתוכו. אותו script יכול לקרוא ולכתוב קבצים ב-Drive, לשלוח מייל דרך חשבון Gmail שלכם, וליצור events בקלנדר שלכם, הכל מאותו קוד JavaScript שכבר רץ על edit-triggers ועל לחיצות תפריט. ה-catch הוא שלכל שירות יש OAuth scope משלו, ו-Apps Script יבקש את המשתמש פעם אחת לכל scope חדש בפעם הראשונה שה-function רצה. שיעור 4 כיסה את משטח `SpreadsheetApp`; השיעור הזה עוסק בשלושת השירותים שנוגעים בכל השאר ב-workspace.

השיעור הזה עובר דרך DriveApp (חיפוש, קריאה, העתקת קבצים), CalendarApp (יצירה וקריאה של events), הפיצול MailApp/GmailApp (מתי לבחור איזה), ו-function עם ארבעה שירותים מעובדת שעושה ingest יומי. האגרגטור הלילי של הצוות הוא בדיוק ה-pattern בין-שירותי בסוף: חיפוש Drive אחד, כתיבת Sheets אחת, event של Calendar אחד, מייל אישור אחד. ברגע שראיתם את כל הארבעה משורשרים, שאר Track 3 מפסיק להציג שירותים חדשים.

## DriveApp: חיפוש, קריאה, כתיבת קבצים

`DriveApp` היא נקודת הכניסה ל-Google Drive. שלוש צורות צצות ב-scripts אמיתיים של הצוות:

```js
// מקבל קובץ לפי ID. ה-ID מגיע מה-URL: drive.google.com/file/d/<ID>/view
const file = DriveApp.getFileById("1abc...");
const csvText = file.getBlob().getDataAsString();   // bytes גולמיים כמחרוזת

// חיפוש לפי שם או לפי query. searchFiles מחזיר iterator, לא מערך.
const iter = DriveApp.searchFiles('title contains "Taboola export"');
while (iter.hasNext()) {
  const f = iter.next();
  Logger.log(f.getName() + " -> " + f.getId());
}

// העתקת קובץ. makeCopy מחזיר את ה-File object החדש; שינוי שם או הזזה אחרי.
const copy = file.makeCopy("Taboola export 2026-05-11", DriveApp.getFolderById("0folder..."));
```

ה-iterator הוא המלכודת הקלה. `searchFiles` ו-`getFolders` שניהם מחזירים Drive iterators, לא מערכים. `.map` ו-`.filter` לא קיימים עליהם: חייבים להניע אותם עם `.hasNext()` ו-`.next()`. לומד שכותב `DriveApp.searchFiles(q).forEach(...)` מקבל שגיאת runtime על "forEach is not a function", וה-bug הוא ה-iterator, לא ה-query.

`DriveApp` צריך את ה-scope `https://www.googleapis.com/auth/drive`. יש `https://www.googleapis.com/auth/drive.file` צר יותר שרואה רק קבצים שהמשתמש מסר במפורש ל-app שלכם (דרך picker או על ידי שנוצרו על ידי ה-script). ה-scope הצר הוא ברירת המחדל הנכונה ל-scripts שנוגעים רק בקבצי הפלט שלהם; מרחיבים רק כשבאמת צריכים לקרוא נתוני משתמש קיימים.

<Compare>
  <ComparePanel label="לא נכון: מתייחס ל-iterator כמערך" tone="negative">
    <pre className="font-mono text-xs"><code>{`const files = DriveApp.searchFiles('title contains "raw"');
files.forEach(function(f) {
  Logger.log(f.getName());
});
// TypeError: files.forEach is not a function`}</code></pre>
    <p className="mt-2 text-sm">ל-Drive iterators אין array methods. <code>.forEach</code>, <code>.map</code>, <code>.filter</code> כולם זורקים.</p>
  </ComparePanel>
  <ComparePanel label="נכון: לולאת hasNext/next" tone="positive">
    <pre className="font-mono text-xs"><code>{`const iter = DriveApp.searchFiles('title contains "raw"');
while (iter.hasNext()) {
  const f = iter.next();
  Logger.log(f.getName());
}`}</code></pre>
    <p className="mt-2 text-sm">עוברים על ה-iterator עם hasNext/next. אם צריך מערך לעיבוד מאוחר, דוחפים לאחד בתוך הלולאה: <code>const all = []; while (iter.hasNext()) all.push(iter.next());</code></p>
  </ComparePanel>
</Compare>

## CalendarApp: קריאה ויצירה של events

```js
const cal = CalendarApp.getDefaultCalendar();   // הקלנדר הראשי של המשתמש
const event = cal.createEvent(
  "Campaign launch: Reverse Mortgage PR (UK)",
  new Date("2026-05-12T09:00:00+03:00"),
  new Date("2026-05-12T09:30:00+03:00"),
  {
    description: "Buyer: Ben Nahum. First $200 of spend goes to LP testing.",
    guests: "ben.nahum@flexelent.com,info@flexelent.com",
    sendInvites: true,
  }
);
```

CalendarApp הוא הקל ביותר מהשלושה. מקבלים Calendar, קוראים ל-`createEvent` (one-off) או `createAllDayEvent` או `createEventSeries` (recurring), וה-event נוחת בקלנדר עם הזמנות אורחים אופציונליות. קריאות עובדות אותו דבר: `cal.getEventsForDay(new Date())` מחזיר מערך (זה כן מערך, לא iterator), וכל Event חושף `getTitle`, `getStartTime`, `getEndTime`, `getDescription`, וכן הלאה.

ה-scope הוא `https://www.googleapis.com/auth/calendar`. script שיוצר רק events למשתמש הרץ (אין נגיעה בקלנדרים אחרים) מסתפק ב-`calendar.events` הצר יותר: שווה להגדיר במניפסט אם ה-script הולך להיות משותף נרחבות.

## MailApp מול GmailApp: פשוט מול עשיר

שני שירותים שולחים מייל. הם נראים דומים במקום הקריאה אבל שונים במה שהם יכולים לעשות ובאיזה scope הם צריכים.

| | `MailApp` | `GmailApp` |
|---|---|---|
| שליחת מייל | כן | כן |
| קריאת inbox / threads | לא | כן |
| צירוף קבצים | כן | כן |
| שינוי drafts, labels, threads | לא | כן |
| Scope נדרש | `script.send_mail` (שליחה בלבד) | `gmail.modify` או `mail.google.com` |
| מכסה יומית (חשבונות consumer) | 100 מיילים / יום | 100 מיילים / יום |
| מכסה יומית (Workspace) | 1,500 מיילים / יום | 1,500 מיילים / יום |

אם ה-script צריך רק לשלוח הודעה יוצאת אחת לכל ריצה, משתמשים ב-`MailApp`. ה-scope צר יותר (לא תיתנו בטעות ל-script גישת קריאה ל-inbox שלכם), ה-API קטן יותר, והמכסה זהה. ה-mailer היומי של ה-spend של הצוות משתמש ב-`MailApp` בדיוק מהסיבה הזו.

```js
MailApp.sendEmail({
  to: "info@flexelent.com",
  subject: "Daily spend report: 2026-05-11",
  body: "Total spend yesterday: $32,418.20\n\nFull breakdown: https://docs.google.com/spreadsheets/...",
});
```

תיק האפשרויות חשוב. הצורות עם ארגומנט יחיד (`MailApp.sendEmail(to, subject, body)`) עובדות אבל נועלות אתכם מ-features כמו `htmlBody`, `cc`, `bcc`, `attachments`, `replyTo`, ו-`name` (שם התצוגה של From, לא הנמען: ראו את המלכודות למטה). ברירת מחדל לצורת ה-object ביום הראשון ולא תצטרכו לכתוב מחדש את הקריאה כשהצוות מבקש פורמט HTML.

### מתי GmailApp באמת שווה את ה-scope הרחב יותר

ברגע שאתם צריכים *לקרוא* מייל (לא רק לשלוח), `MailApp` יוצא מהמשחק. ה-job הלילי של הצוות "שומרים את ה-attachments של חשבוניות Tonic ל-Drive" הוא המקרה הקנוני: מחפשים ב-inbox את ה-thread היומי של החשבונית, עוברים על ההודעות, מושכים כל PDF attachment, שומרים אותו לתיקיית Drive כך שגיליון הפיננסים יוכל להרים אותו.

```js
function archiveTonicInvoices() {
  const threads = GmailApp.search('from:billing@tonic.com has:attachment newer_than:2d');
  const folder = DriveApp.getFolderById("0folder...");
  for (const thread of threads) {
    for (const msg of thread.getMessages()) {
      for (const att of msg.getAttachments()) {
        if (att.getContentType() !== "application/pdf") continue;
        folder.createFile(att.copyBlob()).setName(
          msg.getDate().toISOString().slice(0, 10) + " " + att.getName()
        );
      }
    }
    thread.addLabel(GmailApp.getUserLabelByName("archived-to-drive"));
  }
}
```

שלוש יכולות GmailApp מופיעות כאן ש-`MailApp` לא יכול להתאים: `GmailApp.search` (אותו syntax של query כמו של סרגל החיפוש של ממשק Gmail), איטרציה על threads והודעות, ושינוי labels על ה-thread ברגע שהעבודה נגמרה. שלב ה-label הוא הטריק הזול ל-idempotency: הריצה של מחר מחפשת `newer_than:2d -label:archived-to-drive` ומדלגת על threads שכבר עיבדה.

## Patterns בין-שירותיים

הכוח המלא של scripts של Workspace מופיע כש-function אחת משרשרת את כל השלושה:

```js
function ingestDailyTaboolaCsv() {
  // 1. Drive: מאתרים את ה-CSV export של היום
  const iter = DriveApp.searchFiles(
    'title contains "Taboola export 2026-05-11" and mimeType = "text/csv"'
  );
  if (!iter.hasNext()) {
    MailApp.sendEmail({
      to: "info@flexelent.com",
      subject: "Taboola export missing for 2026-05-11",
      body: "No CSV found in Drive. The Taboola scheduled export probably failed; check the platform dashboard.",
    });
    return;
  }
  const csv = Utilities.parseCsv(iter.next().getBlob().getDataAsString());

  // 2. SpreadsheetApp: כותבים את השורות
  const sheet = SpreadsheetApp.getActive().getSheetByName("Taboola raw");
  sheet.clear();
  sheet.getRange(1, 1, csv.length, csv[0].length).setValues(csv);

  // 3. Calendar: מתזמנים את פגישת הסקירה
  CalendarApp.getDefaultCalendar().createEvent(
    "Taboola data review",
    new Date("2026-05-11T17:00:00+03:00"),
    new Date("2026-05-11T17:30:00+03:00"),
    { description: "Review newly ingested Taboola rows in the Aggregator tab." }
  );

  // 4. MailApp: מאשרים שה-ingest נחת
  MailApp.sendEmail({
    to: "info@flexelent.com",
    subject: "Taboola export ingested: " + (csv.length - 1) + " rows",
    body: "Review meeting on the calendar at 17:00.",
  });
}
```

ארבעה שירותים, function אחת, trigger אחד. בפעם הראשונה שבעל הצוות מריץ אותה, Apps Script מבקש scopes של Drive, Sheets, Calendar, ו-Gmail כולם ביחד. מאז זה רץ בשקט.

<StepThrough
  frames={[
    {
      caption: "שלב 1: חיפוש Drive ב-02:00",
      body: (
        <>
          ה-trigger יורה. <code>DriveApp.searchFiles</code> מחפש את ה-Taboola
          export של היום. <code>iter.hasNext()</code> הוא true: ה-CSV שם.
        </>
      ),
    },
    {
      caption: "שלב 2: קריאה וניתוח",
      body: (
        <>
          <code>iter.next().getBlob().getDataAsString()</code> מושך את ה-bytes
          של ה-CSV כמחרוזת. <code>Utilities.parseCsv</code> הופך את המחרוזת
          למערך דו-ממדי.
        </>
      ),
    },
    {
      caption: "שלב 3: כתיבה ל-sheet",
      body: (
        <>
          <code>setValues</code> אחד כותב את כל המערך הדו-ממדי ל-tab Taboola raw.
          אפילו עם 5,000 שורות זה round-trip אחד ומסיים תוך שנייה.
        </>
      ),
    },
    {
      caption: "שלב 4: תזמון והודעה",
      body: (
        <>
          <code>createEvent</code> מציב event סקירה של 17:00 בקלנדר.
          <code>MailApp.sendEmail</code> שולח אישור. ה-trigger handler יוצא.
          סך זמן ריצה: ~3 שניות לריצה לילית נקייה.
        </>
      ),
    },
  ]}
/>

<QuickCheck
  question="אתם כותבים DriveApp.searchFiles('title contains raw').forEach(processFile). אתם מקבלים שגיאת runtime: forEach is not a function. מה הסיבה?"
  options={[
    {
      id: "a",
      label: "ל-query החיפוש יש syntax לא חוקי; .forEach בסדר על התוצאות.",
      correct: false,
      explanation: "ה-query בסדר. הבעיה היא ש-searchFiles מחזיר iterator, לא מערך, ול-iterators אין array methods.",
    },
    {
      id: "b",
      label: "DriveApp.searchFiles מחזיר iterator, שאין לו array methods. עוברים עליו עם hasNext/next, או אוספים למערך קודם.",
      correct: true,
      explanation: "נכון. Drive iterators עושים paging פנימי ואין להם .forEach, .map, .filter. משתמשים ב-while (iter.hasNext()) { const f = iter.next(); ... }.",
    },
    {
      id: "c",
      label: "V8 runtime של Apps Script לא תומך ב-.forEach על אוסף קבצים; חייבים להשתמש בלולאת for.",
      correct: false,
      explanation: ".forEach עובד על מערכים רגילים ב-V8. הבעיה היא iterator מול מערך, לא תמיכת מנוע.",
    },
  ]}
/>

## עכשיו לעבודה אמיתית

<MiniGrid
  data={CAMPAIGNS_LARGE.slice(0, 6)}
  caption="לוג ה-Campaigns תופס A1:G61. ה-spend הכולל חושב מראש ויושב ב-J1. המשימה שלכם: לקרוא את J1 ולשלוח מייל דוח spend יומי."
/>

הקובץ ההתחלתי `Lesson.gs` מצהיר על `function emailSpendReport()` עם TODO. שלושה צעדים:

1. קוראים את סכום ה-spend מ-J1 ב-tab של Campaigns: `const spend = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("Campaigns").getRange("J1").getValue();`.
2. שולחים מייל עם `MailApp.sendEmail({ to: ..., subject: ..., body: ... })`. הנמען יכול להיות `info@flexelent.com` או כל כתובת שאתם שולטים בה.
3. וודאו שה-body באמת כולל את הסכום (template literal: `` `Total spend today: $${spend}` ``).

לוחצים Run על `emailSpendReport` בעורך. מקבלים את ה-prompt של scope ה-gmail בפעם הראשונה. מריצים שוב; בודקים את ה-inbox.

## נפוץ בשטח

לצוות יש שלושה scripts שנוגעים ב-Workspace שרצים כל יום:

- **התקציר היומי של ה-spend.** trigger מבוסס-זמן קורא ל-`emailSpendReport` ב-09:00 עם הסכום של היום הקודם, שלוש ה-verticals הגבוהות ביותר, ושלוש הנמוכות ביותר. הולך לכל media buyer פעיל.
- **ניקוי Drive של יום שישי.** query של `searchFiles` מוצא כל קובץ "raw export" ישן מ-30 יום, מעתיק אותם לתיקיית ארכיון, ומסיר את המקורות. חוסך את מכסת ה-Drive של הצוות ל-exports של השבוע הנוכחי.
- **drops של launch קמפיין ב-קלנדר.** כש-buyer לוחץ "Launch" ב-sidebar מותאם (שיעור 9 מכסה את ה-pattern של ה-sidebar), ה-script יוצר event ב-Calendar 24 שעות קדימה כ-check-in סקירה placeholder.

## מלכודות מקצועיות

- **שדה `name` של `MailApp.sendEmail` הוא שם התצוגה של ה-FROM, לא הנמען.** קוד מתחיל שקורא `MailApp.sendEmail({ to: "info@flexelent.com", name: "info@flexelent.com", ... })` שולח הודעה מ-"info@flexelent.com" (תצוגה) ול-"info@flexelent.com": שעובד, אבל לא מה שהכותב חשב שהוא עושה. הנמען הוא תמיד `to:` (או `bcc:` / `cc:`).
- **`DriveApp.searchFiles` מחזיר iterator, לא מערך.** אין `.map`, אין `.forEach`, אין `.length`. Drive iterators הם streams; עוברים עליהם עם `.hasNext()` / `.next()` ואוספים למערך בעצמכם אם צריך אחד.
- **מכסות שליחת Gmail מחמירות ממה שהן נראות** (שיעור 16 מכסה מכסות פר-שירות ואסטרטגיות batching לעומק). 100 / יום לחשבונות consumer, 1,500 / יום ל-Workspace, והן מתאפסות בחלון מתגלגל של 24 שעות, לא בחצות. script שעושה לולאה של `sendEmail` 100 פעמים בדקה אחת יפגע במגבלת קצב השליחה פר-דקה לפני שיפגע במגבלה היומית ויתחיל לזרוק. עוטפים להודעה אחת עם body ארוך יותר, או מגבילים קצב עם `Utilities.sleep(1000)` בין שליחות.
- **`DriveApp.createFile` ב-bound script שם את הקובץ ב-Drive של בעל ה-SCRIPT, לא ב-Drive של המשתמש הרץ.** אם ה-script שלכם בבעלות יואב ומורץ על ידי דינה, הקובץ נוחת ב-Drive של יואב עם דינה כעורכת (אם זכרתם לשתף אותו). כדי ליצור ב-Drive של המשתמש הרץ, משתמשים ב-`Drive.Files.create(...)` עם שירות Drive המתקדם במקום: הוא רץ כמשתמש.
- **`CalendarApp.getDefaultCalendar()` מחזיר את הקלנדר שבבעלות מי שמריץ את ה-script, לא של בעל ה-script.** ב-script עם "Execute as me" בעל ה-script הוא "me", אז הריצה של דינה עדיין יוצרת events בקלנדר של יואב. שני מצבי ההרצה ("as me" מול "as the user accessing") חשובים כאן בדיוק כמו ב-web apps בשיעור 13.
- **בקשת `https://mail.google.com/` (גישת Gmail מלאה) כשרק שולחים מייל.** ה-prompt של ההרשאה של Apps Script מציג את התיאור המלא של ה-scope, ו-"Read, compose, send, and permanently delete all your email" מבריח משתמשים מהרצת ה-script. יורדים ל-`script.send_mail` אם כל מה שאתם עושים זה לשלוח.
- **`Utilities.parseCsv` שלא מכבד delimiters מותאמים.** כברירת מחדל הוא מפצל בפסיק. ל-TSV (חלק מהפלטפורמות exports מופרדי-tab), מעבירים `Utilities.parseCsv(str, "\t")`. bug נפוץ הוא ניתוח TSV עם ה-delimiter ברירת המחדל של פסיק וקבלת עמודה אחת עם הכל מודבק ב-tab.
- **timezones של event ב-Calendar שנסחפים.** `new Date("2026-05-12T09:00:00")` מנתח כ-timezone של ה-script אם אין offset; מעבירים `+03:00` (ישראל) או `-04:00` (NYC) במפורש כך שה-event ינחת בזמן שעון נכון ללא קשר לאיזה משתמש מריץ את ה-function.
