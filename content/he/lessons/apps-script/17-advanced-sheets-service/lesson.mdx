import { CAMPAIGNS_LARGE } from "@/content/datasets/adtech";

# Apps Script + ה-Sheets advanced service: כש-SpreadsheetApp לא מספיק

`SpreadsheetApp` הוא ה-surface הידידותי שרוב Track 3 השתמש בו: objects ברמה גבוהה, ברירות מחדל שפויות, OAuth מטופל implicitly. הוא מכסה בערך 80% ממה שהצוות עושה ב-bound script. ה-20% הנותרים (יצירת pivot tables תכנותית, conditional formatting בכמויות, copy-paste בין spreadsheets, כל דבר שנוגע ב-Sheets v4 REST API ישירות) צריך את ה-**Sheets advanced service**. זה global דק שנקרא `Sheets` שממפה 1:1 ל-endpoints של Sheets v4 REST, חושף יותר עוצמה, ומחליף קצת נוחות בשליטה.

השיעור הזה מכסה מתי להושיט יד ל-advanced service מול `SpreadsheetApp` רגיל, איך לאפשר אותו (רשומת manifest), הצורה של `Sheets.Spreadsheets.values.get` ו-`update`, ה-`batchUpdate` עבור כל דבר שאין לו `values.*` קיצור, וצורות ה-response שמאפשרות לכם לשרשר עבודת המשך. אחרי השיעור הזה Track 3 גמור: יש לכם את המשטח המלא של Apps Script לחבר כל אוטומציה של workbook שהצוות צריך.

## מתי להושיט יד ל-advanced service

| משימה | `SpreadsheetApp` | Advanced service |
|---|---|---|
| לקרוא כמה תאים, להגדיר ערך | שורה אחת | מוגזם |
| לעבור על sheet של 5,000 שורות | עם batching | אותו רעיון, מעט מהיר יותר |
| ליצור pivot table תכנותית | לא נתמך | `Sheets.Spreadsheets.batchUpdate` עם request `addPivotTable` |
| להוסיף 20 חוקי conditional-format בבת אחת | איטי (קריאה אחת לחוק) | `batchUpdate` אחד עם 20 איברי `addConditionalFormatRule` |
| להעתיק פורמט מ-sheet A ל-sheet B (workbooks שונים) | מסורבל | `batchUpdate` עם requests של `copyPaste` |
| להריץ request של Sheets API בדרך שה-REST docs מתעדים | משטח לא תואם | משקף את צורת ה-REST |

אם אי פעם קראתם את [Sheets API REST reference](https://developers.google.com/sheets/api) וחשבתם "הלוואי שיכולתי לקרוא לזה מ-Apps Script", ה-advanced service הוא מה שמגשר על הפער. כל דבר ב-REST docs יש לו מקבילה ב-Sheets advanced service.

## הפעלה (ריקוד manifest)

ה-advanced service חייב להיכנס פר-script. שתי דרכים לעשות את זה:

1. **UI של העורך**: ברצועת השמאל של עורך Apps Script, לוחצים **Services then + then Google Sheets API then Add**. העורך מוסיף רשומה ל-manifest שלכם אוטומטית וסיימתם.
2. **עריכת manifest**: פותחים את `appsscript.json` (Project Settings then מסמנים "Show appsscript.json manifest file in editor") ומוסיפים את רשומת ה-`dependencies.enabledAdvancedServices` בעצמכם:

```json
{
  "timeZone": "America/New_York",
  "dependencies": {
    "enabledAdvancedServices": [
      {
        "userSymbol": "Sheets",
        "serviceId": "sheets",
        "version": "v4"
      }
    ]
  }
}
```

השדות:

| שדה | מה זה עושה |
|---|---|
| `userSymbol` | שם ה-global שהעורך יוצר. לפי מוסכמה `Sheets` כך שהקוד שלכם קורא `Sheets.Spreadsheets.values.get(...)`. אפשר לשנות; אף אחד לא עושה. |
| `serviceId` | ה-service ID הפנימי של Google. ל-Sheets זה `"sheets"`. שירותים advanced אחרים: `"drive"`, `"gmail"`, `"calendar"`. |
| `version` | גרסת API. Sheets ב-`v4`. |

אחרי שמירה, העורך מפעיל את ה-API התואם ב-project GCP הבסיסי אוטומטית (עבור projects "default"). כשאתם מריצים את ה-function בפעם הראשונה, ה-OAuth consent prompt כולל עכשיו את ה-scope הרחב יותר שה-advanced service צריך.

## הצורה של קריאות advanced-service

שתי ה-methods שמכסות את רוב עבודת האדטק:

```js
function readSheetValues() {
  const spreadsheetId = SpreadsheetApp.getActiveSpreadsheet().getId();
  const response = Sheets.Spreadsheets.values.get(
    spreadsheetId,
    "Campaigns!F2:G61"
  );
  // response = { range: "Campaigns!F2:G61", majorDimension: "ROWS", values: [[412.5, 587.4], ...] }
  const rows = response.values || [];
  return rows;
}

function writeOneCell() {
  const spreadsheetId = SpreadsheetApp.getActiveSpreadsheet().getId();
  Sheets.Spreadsheets.values.update(
    { values: [[12345]] },         // body של request
    spreadsheetId,
    "Campaigns!J1",                // A1 range (כולל שם ה-sheet)
    { valueInputOption: "RAW" }    // פרמטרי query
  );
}
```

שלושה דברים ששווה להצמיד:

- **סדר הארגומנטים תואם ל-REST.** body קודם (כשיש), אז path params, אז query params. methods של קריאה אין להם body ומתחילים עם path params.
- **Ranges כוללים את שם ה-sheet.** `"Campaigns!F2:G61"`, לא `"F2:G61"`. ה-advanced service לא יודע על ה-active sheet בדרך ש-`SpreadsheetApp.getActiveSheet()` יודע.
- **`valueInputOption` חשוב.** `"RAW"` כותב את הערך כמו שהוא. `"USER_ENTERED"` מנתח אותו כאילו משתמש הקליד אותו (אז `"=SUM(...)"` הופך לנוסחה, `"5/12/2026"` הופך לתאריך). ברירת המחדל ב-REST API היא ה-`"RAW"` השמרני יותר, אבל שווה להגדיר במפורש.

<Compare>
  <ComparePanel label="SpreadsheetApp: ברור, ללא pivot" tone="neutral">
    <pre className="font-mono text-xs"><code>{`const sheet = SpreadsheetApp.getActive()
  .getSheetByName("Campaigns");
const vals = sheet.getRange("F2:G61").getValues();

// כדי ליצור pivot table:
// לא נתמך. הייתם צריכים לכתוב script שמלווה
// את המשתמש דרך Data → Pivot table ידנית.`}</code></pre>
    <p className="mt-2 text-sm">ברור וארגונומי למקרה הנפוץ. אבל אין API תכנותי ל-pivots, לקונדישינל פורמטס בכמויות, או לכל דבר שחי בשכבת REST.</p>
  </ComparePanel>
  <ComparePanel label="Advanced service: עוצמה, יותר מפורט" tone="positive">
    <pre className="font-mono text-xs"><code>{`const id = SpreadsheetApp.getActive().getId();
const res = Sheets.Spreadsheets.values.get(
  id, "Campaigns!F2:G61"
);
const vals = res.values || [];

// יצירת pivot table:
Sheets.Spreadsheets.batchUpdate(
  { requests: [{ updateCells: { ... pivot ... } }] },
  id
);`}</code></pre>
    <p className="mt-2 text-sm">יותר מפורט לקריאות פשוטות, אבל פותח pivots, conditional formatting בכמויות, copyPaste בין-workbooks, וכל דבר אחר שה-REST API תומך בו.</p>
  </ComparePanel>
</Compare>

## `batchUpdate` הוא הסוס העובד

ה-surface האחר הוא `Sheets.Spreadsheets.batchUpdate`, שמקבל מערך של **requests** בקריאה אחת. ה-requests יכולים לעשות הכל: להוסיף sheet, להוסיף pivot table, להגדיר conditional formats, להקפיא שורות, להסתיר עמודות, copy/paste בין ranges. דוגמה:

```js
Sheets.Spreadsheets.batchUpdate(
  {
    requests: [
      { addSheet: { properties: { title: "Monthly Rollup" } } },
      { addConditionalFormatRule: { rule: { /* ... */ } } },
      { repeatCell: { range: { /* ... */ }, cell: { /* ... */ }, fields: "userEnteredFormat" } },
    ],
  },
  spreadsheetId,
);
```

round-trip אחד מיישם את כל שלושת ה-requests אטומית. מוסיפים 20 חוקי conditional-format עם 20 רשומות במערך; מקבלים קריאת רשת אחת במקום 20. ה-script של בונה ה-pivot של הצוות שנהג לקחת 90 שניות (יצירת sheets אחד בכל פעם, הגדרת פורמטים אחד בכל פעם) עכשיו לוקח 4 שניות עם הכל ב-`batchUpdate` יחיד.

<StepThrough
  frames={[
    {
      caption: "שלב 1: בונים את מערך ה-requests",
      body: (
        <>
          אתם בונים מערך של request objects ב-JS:{" "}
          <code>[addSheet, addConditionalFormatRule, repeatCell, ...]</code>.
          אין עדיין קריאות רשת; המערך חי בזיכרון של ה-script שלכם.
        </>
      ),
    },
    {
      caption: "שלב 2: קריאת batchUpdate אחת",
      body: (
        <>
          <code>Sheets.Spreadsheets.batchUpdate({"{requests}"}, id)</code>{" "}
          שולח את כל המערך כ-RPC יחיד. Apps Script מעביר את זה ל-endpoint
          Sheets v4 REST, שמיישם את ה-requests אטומית.
        </>
      ),
    },
    {
      caption: "שלב 3: response נושא reply objects",
      body: (
        <>
          ה-response הוא{" "}
          <code>{`{ replies: [{ addSheet: { properties: { sheetId } } }, ...] }`}</code>.
          כל רשומה ב-<code>replies</code> תואמת ל-request המקביל.
          ה-<code>sheetId</code> של ה-sheet החדש נמצא ב-reply, שאתם יכולים
          להשתמש בו לשרשר requests המשך.
        </>
      ),
    },
    {
      caption: "שלב 4: שרשור המשך אם צריך",
      body: (
        <>
          <code>batchUpdate</code> שני יכול עכשיו להשתמש ב-sheetId משלב 3
          כדי להוסיף pivot tables, להקפיא שורות וכו', על ה-sheet החדש שנוצר.
          השרשור הוא מה שהופך את ה-advanced service לחזק.
        </>
      ),
    },
  ]}
/>

## קריאת ה-response

כל קריאה של advanced-service מחזירה JS object רגיל שתואם לצורת response של REST. `values.get` מחזיר `{ range, majorDimension, values }`. `batchUpdate` מחזיר `{ spreadsheetId, replies, updatedSpreadsheet? }`, כאשר `replies` הוא מערך של תוצאות פר-request (ה-reply של request `addSheet` כולל את ה-`sheetId` של ה-sheet החדש, שאתם יכולים לשרשר ל-request המשך).

זה החלק שעושה את ה-advanced service חזק. ה-reply object נושא את ה-IDs ואת ה-metadata שאתם צריכים לשרשר עבודה נוספת, בדרך ש-`Sheet` objects של `SpreadsheetApp` לא יכולים.

<QuickCheck
  question="אתם רוצים להוסיף תכנותית pivot table ל-tab Aggregator כל יום שני. איזה API יכול לעשות את זה?"
  options={[
    {
      id: "a",
      label: "SpreadsheetApp.getRange().createPivotTable(...).",
      correct: false,
      explanation: "ל-SpreadsheetApp אין method ליצירת pivot tables. ה-advanced service הוא האפשרות התכנותית היחידה.",
    },
    {
      id: "b",
      label: "Sheets.Spreadsheets.batchUpdate עם updateCells request שמספק שדה pivotTable.",
      correct: true,
      explanation: "נכון. ה-Sheets advanced service חושף את Sheets v4 REST API ישירות. יצירת pivot היא updateCells request עם שדה pivotTable על התא. SpreadsheetApp לא תומך בזה כלל.",
    },
    {
      id: "c",
      label: "Sheets.Spreadsheets.values.update עם valueInputOption: 'USER_ENTERED' ונוסחה PIVOT() מיוחדת.",
      correct: false,
      explanation: "אין נוסחת PIVOT(). pivots הם תכונה מבנית של ה-workbook, לא נוסחת תא; הם נוצרים דרך ה-API של batchUpdate.",
    },
  ]}
/>

## עכשיו לעבודה אמיתית

<MiniGrid
  data={CAMPAIGNS_LARGE.slice(0, 6)}
  caption="לוג ה-Campaigns תופס A1:G61. השיעור מסכם את כל עמודת Spend דרך ה-advanced service וכותב את הסכום ל-J1."
/>

שני חלקים לקטע המעשי הזה:

1. **מאפשרים את ה-Sheets advanced service** ב-manifest. לוחצים **Services then + then Google Sheets API then Add** בעורך. מאשרים ש-`appsscript.json` עכשיו יש לו את רשומת `dependencies.enabledAdvancedServices` עם `serviceId: "sheets"`.
2. **מיישמים `batchUpdateUsingAdvancedService`.** ההתחלה ריקה. קוראים `F2:G61` דרך `Sheets.Spreadsheets.values.get`, מסכמים את האיבר הראשון של כל שורה מוחזרת (זה ה-spend; השני הוא ה-revenue), וכותבים את הסכום ל-J1 דרך `Sheets.Spreadsheets.values.update` עם `valueInputOption: "RAW"`.

בפעם הראשונה שאתם מריצים את זה, מקבלים את ה-OAuth scope הרחב יותר. מאז ה-advanced service זמין על פני ה-project, כולל מ-functions ו-triggers אחרים.

## נפוץ בשטח

- **מחולל pivot תכנותי** (שיעורים 2.2 ו-2.3 מכסים את אותם מנגנוני pivot מצד ה-UI). הצוות צריך pivot פר-buyer-פר-platform שמתחולל מחדש כל יום שני על נתוני השבוע הנוכחי. ל-`SpreadsheetApp` אין method ליצירת pivot; ל-`Sheets.Spreadsheets.batchUpdate` עם request `updateCells` שמספק שדה `pivotTable` יש. trigger בוקר יום שני אחד, קריאת batchUpdate אחת, pivot טרי.
- **העתקת פורמט בין-workbook.** כשנוצר sheet dashboard חודשי חדש (מ-template), הפורמט של ה-sheet של חודש שעבר חייב להיות מועתק: conditional formats, frozen rows, רוחבי עמודות. `batchUpdate` יחיד עם requests של `copyPaste` (`pasteType: "PASTE_FORMAT"`) מטפל בכל זה; עם `SpreadsheetApp` זה היה עשר קריאות נפרדות.
- **Conditional formatting בכמויות על עמודות חודשיות חדשות** (שיעור 2.6 מכסה את בונה החוקים ב-UI). כל חודש מוסיפים עמודה חדשה של spend / revenue / ROI ל-sheet פר-buyer. 20 חוקי conditional-format לתחזק (ירוק אם ROI > 1.5, צהוב אם 1.0-1.5, אדום מתחת). `batchUpdate` יחיד עם 20 רשומות `addConditionalFormatRule`; לוקח 2 שניות מול 40 עם המסלול פר-חוק של `SpreadsheetApp`.
- **Sheets API pagination על ranges ענקיים.** range של 500,000 תאים חורג ממגבלת גודל response של Apps Script כשנקרא דרך `SpreadsheetApp`. ה-advanced service תומך ב-paging דרך ranges בחלקים (`A1:A50000`, `A50001:A100000`, ...); כל קריאה מקבלת response ניתן לניהול.

## מלכודות מקצועיות

- **`Sheets is not defined`** בגוף ה-function אומר שרשומת ה-manifest עדיין לא שם. פאנל ה-Services של העורך מוסיף אותה; שמירת ה-manifest ביד גם עובדת. השגיאה לא יורה עד שה-function רצה, אז לא בוטחים ב-"זה מתקמפל"; מריצים פעם אחת לאימות.
- **`valueInputOption: "USER_ENTERED"` מנתח באגרסיביות.** כתיבת המחרוזת `"5/12/2026"` כ-`USER_ENTERED` מפורשת כתאריך (12 במאי ב-locale ארה"ב, 5 בדצמבר ב-locale אירופי, תלוי ב-workbook). עבור טקסט גולמי ומספרים מחושבים מראש, `"RAW"` בטוח יותר. עבור ערכים שאמורים להתנהג כאילו הוקלדו (נוסחאות כמו `"=SUM(F2:F61)"`), `"USER_ENTERED"` נדרש.
- **שירותים advanced נספרים מול אותן מכסות פר-יום כמו `SpreadsheetApp` פלוס מכסות Sheets API.** ל-Sheets API יש מכסה פר-project משלו (300 read requests/דקה, 60 write requests/דקה, free tier). trigger ש-batchUpdates 100 פעמים בשעה יכול לפגוע במגבלת ה-write; ה-API מחזיר 429 וה-script שוגה עם `Quota exceeded for quota metric 'Write requests'`. משתמשים ב-`batchUpdate` עם מספר requests במקום קריאות `batchUpdate` מרובות.
- **bodies של request batchUpdate גדולים ומחרוזתיים.** כשמשהו משתבש, הודעת השגיאה שמית אינדקס request אבל ללא פירוט. התיקון הוא `console.log(JSON.stringify(requests, null, 2))` לפני הקריאה כך שתוכלו לראות את ה-payload המדויק. הדבקה ב-sandbox "Try it" של Sheets API לרוב מציפה את אי-התאמת ה-schema מהר יותר מהרצה מחדש ב-Apps Script.
- **ה-advanced service של Drive יותר יקר מ-Sheets.** כשמפעילים את ה-advanced service של Drive לעבור על תיקיות או לשנות metadata של קבצים, כל קריאה מחזירה payload metadata כבד כברירת מחדל. משתמשים בפרמטר `fields` (`{ fields: "id, name" }`) כדי להקטין את ה-response. בלעדיו, איטרציה של 500 קבצים יכולה לפגוע במגבלות גודל response.
- **הפעלת advanced service מרחיבה OAuth scopes.** מסך ההרשאה של ה-script עכשיו מבקש גישה רחבה יותר (למשל, "See and modify your Sheets"). ב-script project משותף, כל משתמש חייב לתת consent מחדש בריצה הבאה. לא מפעילים advanced services בקלילות על scripts של production שכל הצוות משתמש; מתאמים את ה-rollout.
- **ה-`userSymbol` שלכם אבל לפי מוסכמה תואם את ה-API.** אתם יכולים לכתוב `"userSymbol": "MySheets"` ואז לקרוא `MySheets.Spreadsheets.values.get(...)`, אבל זה מבלבל. משאירים אותו כ-`"Sheets"` כך שהקוד שלכם תואם ל-docs ולתשובות ב-stack overflow.
- **`values.get` מחזיר `undefined` עבור `response.values`.** כשה-range המבוקש ריק לחלוטין, ה-response משמיט את שדה ה-`values` לחלוטין. תמיד עושים default עם `response.values || []` כך שהקוד במורד לא יתפוצץ במקרה של sheet ריק.
