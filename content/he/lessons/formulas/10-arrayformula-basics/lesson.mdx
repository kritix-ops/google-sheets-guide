import { CAMPAIGNS } from "@/content/datasets/adtech";

# Array formulas: יסודות ARRAYFORMULA

`ARRAYFORMULA` הוצג בקצרה בשיעור 2 כפתרון של Sheets ל-auto-spill של Excel. השיעור הזה נכנס לעומק של מתי להישען עליו, מתי לא, ואיך להשתמש בו בלי להפיל את הגיליון על הברכיים על נתונים גדולים. בעבודת אדטק, ARRAYFORMULA הוא הדרך לשמור על עמודות מחושבות (רווח, ROI, EPC, שמות מנורמלים) שניתנות לתחזוקה על דאטה-סט שגדל כל יום: נוסחה אחת בראש, בלי צורך למלא למטה.

<FunctionRef name="ARRAYFORMULA" />

## מה הוא עושה ולמה צריך אותו

ב-Excel 365, `=B2:B13 - A2:A13` עושה spill ל-12 תאים אוטומטית. Sheets מחזיר רק את הערך הראשון. עטיפה ב-`ARRAYFORMULA` אומרת ל-Sheets להחיל את הפעולה על פני כל הטווח.

<StepThrough
  frames={[
    {
      caption: "שלב 1: קוראים את הביטוי הפנימי",
      body: (
        <>
          המנוע רואה <code>F2:F13 - E2:E13</code>. בלי ARRAYFORMULA זה מתכווץ לערך בודד (חיסור של התא הראשון של כל טווח). העטיפה מעבירה את המנוע למצב מערך.
        </>
      ),
    },
    {
      caption: "שלב 2: מרחיבים כל טווח למערך באותה צורה",
      body: (
        <>
          <code>F2:F13</code> הופך למערך 12-אלמנטי של ערכי revenue. <code>E2:E13</code> הופך למערך 12-אלמנטי מקביל של ערכי spend. אותה צורה, מיושרים שורה אחר שורה.
        </>
      ),
    },
    {
      caption: "שלב 3: מחילים את האופרטור איבר-איבר",
      body: (
        <>
          שורה 1 פחות שורה 1, שורה 2 פחות שורה 2, וכך עד שורה 12. הפלט הוא מערך 12-אלמנטי של רווחים.
        </>
      ),
    },
    {
      caption: "שלב 4: עושים spill לתוצאה",
      body: (
        <>
          המערך נכתב לתוך 12 תאים החל מתא הנוסחה. אם תא יעד אינו ריק, כל הנוסחה נכשלת ב-<code>#REF!</code>: התנגשות spill.
        </>
      ),
    },
  ]}
/>

### דוגמה 1: נתוני צעצוע

שתי עמודות קטנות, נוסחה אחת. צפו איך `ARRAYFORMULA` הופך ביטוי בודד לעמודה.

<RefDemo
  data={[
    ["Item", "Price", "Qty"],
    ["A", 10, 3],
    ["B", 20, 2],
    ["C", 15, 4],
  ]}
  formula="=ARRAYFORMULA(B2:B4*C2:C4)"
  formulaCell="D2"
  fillTo="D4"
  expectedFills={["=ARRAYFORMULA(B2:B4*C2:C4)", "", ""]}
  caption="נוסחה אחת ב-D2 ממלאת את D2:D4 בסך הכל של השורה (price כפול qty)."
/>

<MiniGrid
  data={CAMPAIGNS}
  caption="לוג הקמפיינים. spend בעמודה E, revenue בעמודה F. נחשב רווח (F − E) לכל שורה מתוך תא בודד."
  highlight={["E2", "F2"]}
/>

### דוגמה 2: נתוני אדטק אמיתיים

<TryIt
  data={CAMPAIGNS}
  inputCell="G1"
  expectedFormula="=ARRAYFORMULA(F2:F13-E2:E13)"
  task={<>חשבו רווח (revenue פחות spend) לכל הקמפיינים בבת אחת. נוסחה אחת ב-<code>G1</code> אמורה למלא את <code>G1:G12</code> ברווח של כל קמפיין.</>}
  hint={<>עטפו את <code>F2:F13-E2:E13</code> ב-<code>ARRAYFORMULA(...)</code> כדי שיעשה spill.</>}
  answer={<>הביטוי הפשוט <code>F2:F13-E2:E13</code> מחזיר רק את הערך הראשון (487.30 − 342.18 = 145.12). עטיפה ב-ARRAYFORMULA אומרת ל-Sheets לחשב את החיסור איבר-איבר על פני שני הטווחים. התוצאה עושה spill ל-G1 עד G12. אם הייתם מקלידים את זה ב-Sheets אמיתי, הייתם שמים לב שאי אפשר לערוך אף תא מ-G2-G12 בנפרד: אלו "spill cells" שנשלטים על ידי הנוסחה הראשית ב-G1. כדי לשנות את הנוסחה, עורכים את G1.</>}
/>

## פונקציות שעושות spill לבד

חלק מהפונקציות של Sheets מודעות למערך מטבען ולא זקוקות ל-`ARRAYFORMULA`:

- `FILTER`, `SORT`, `UNIQUE`
- `QUERY`
- `SEQUENCE`, `RANDARRAY`
- `SPLIT`, `TRANSPOSE`, `FLATTEN`

כל דבר אחר, כולל אופרטורים בסיסיים (`+`, `-`, `*`, `/`), השוואות (`>`, `=`), פונקציות טקסט (`LEFT`, `UPPER`, `&`), ורוב הפונקציות האחרות כשמופעלות על טווח, זקוק לעטיפה.

<QuickCheck
  question="איזו מהאפשרויות הבאות לא צריכה ARRAYFORMULA כדי למלא תאים מרובים?"
  options={[
    {
      id: "a",
      label: <code>=UPPER(B2:B13)</code>,
      correct: false,
      explanation: "UPPER על טווח מחזיר רק את הערך הראשון באותיות גדולות אלא אם עוטפים. זקוק ל-ARRAYFORMULA.",
    },
    {
      id: "b",
      label: <code>=FILTER(F2:F13, B2:B13 = "Yoav Cohen")</code>,
      correct: true,
      explanation: "FILTER מודע למערך. כך גם SORT, UNIQUE, QUERY, SEQUENCE, RANDARRAY, SPLIT, TRANSPOSE, ו-FLATTEN.",
    },
    {
      id: "c",
      label: <code>=F2:F13/E2:E13</code>,
      correct: false,
      explanation: "חילוק רגיל בין טווחים מחזיר רק את התא הראשון. זקוק ל-ARRAYFORMULA.",
    },
  ]}
/>

## לא לעטוף מה שכבר מודע למערך

עטיפה של `FILTER`, `QUERY` וכו' ב-`ARRAYFORMULA` לא תיתן שגיאה, פשוט מיותרת. אבל זה יכול לבלבל קוראים ומוסיף רעש. הקונבנציה: עוטפים רק את מה שצריך עטיפה.

```
=FILTER(F2:F13, B2:B13 = "Yoav Cohen")           ✓ נקי
=ARRAYFORMULA(FILTER(F2:F13, B2:B13 = "Yoav Cohen"))  ✗ רועש
```

## תבנית "הטווח הפתוח"

בעבודת אדטק אמיתית, אי אפשר לדעת כמה קמפיינים יתווספו. קיבוע הטווח ל-`E2:E13` עובד היום, אבל בשבוע הבא יהיו שורות 14, 15, 16... וה-ARRAYFORMULA שלכם מפסיקה לכסות אותן.

התבנית: להשתמש בטווח פתוח כמו `E2:E` (כל מה שמ-E2 עד תחתית העמודה) או לקבוע גבול עליון גבוה כמו `E2:E10000`. עם `ARRAYFORMULA`, זה אומר "חשב לכל שורה שיש בה נתונים, ועצור".

```
=ARRAYFORMULA(IF(E2:E="", "", F2:F - E2:E))
```

ה-guard ב-`IF(E2:E="", "", ...)` מדכא פלט עבור שורות ריקות. בלעדיו, הנוסחה עושה spill של "0" או "#DIV/0!" כל הדרך עד שורה מיליון. איתו, הערכים שעושים spill נעצרים היכן שהנתונים נעצרים.

<TryIt
  data={CAMPAIGNS}
  inputCell="G1"
  expectedFormula='=ARRAYFORMULA(IF(E2:E="","",F2:F-E2:E))'
  task={<>בנו עמודת רווח שמטפלת בשורות חדשות שמתווספות. השתמשו ב-<code>E2:E</code> (פתוח) והגנו מפני שורות ריקות עם IF.</>}
  hint={<>עטפו את הכל ב-ARRAYFORMULA. בפנים, השתמשו ב-<code>IF(E2:E="", "", F2:F - E2:E)</code>: מחזירים מחרוזת ריקה לשורות בלי spend, אחרת מחשבים רווח.</>}
  answer={<>הטווח הפתוח <code>E2:E</code> מכסה כל שורה מ-E2 עד תחתית הגיליון. ARRAYFORMULA מחיל את ה-IF/חיסור על כל הטווח. ה-guard של IF מחזיר ריק לשורות שבהן E ריק, אז הפלט שעושה spill נעצר בשורת הנתונים האחרונה במקום למלא את כל המיליון שורות בשגיאות. זאת התבנית לכל עמודה מחושבת בגיליון שגדל לאורך זמן. בלי ה-guard, התחתית של עמודה G הייתה מתמלאת ב-#DIV/0! או 0-ים, מה שמאט את הגיליון ומבלבל lookups שמתבססים עליו.</>}
/>

## ARRAYFORMULA + IF: הסתעפות על פני שורות

`IF` עובד בתוך `ARRAYFORMULA` כדי להחיל לוגיקה שונה לכל שורה. התנאי יכול להיות השוואה מול טווח, ושני הענפים יכולים להיות ביטויים שכוללים טווחים אחרים.

השימוש הקלאסי באדטק: עמודת "Status" שמתייגת את הרווחיות של כל קמפיין. נוסחה אחת בראש, ממלאת את כל העמודה.

```
=ARRAYFORMULA(IF(F2:F-E2:E > 0, "Profit", "Loss"))
```

אותו רעיון עם IFS: לחלק כל שורה לקבוצה לפי ROI בנוסחה אחת. (שימו לב: IFS בתוך ARRAYFORMULA עובד ב-Sheets אבל יכול להיות איטי יותר מ-IF מקונן מקביל על גיליונות ענקיים.)

### מלכודות נפוצות

<Compare>
  <ComparePanel label="שבור" tone="negative">
    <pre className="font-mono text-xs"><code>=ARRAYFORMULA(SUMIF(B2:B13, "Yoav", F2:F13))</code></pre>
    <p className="mt-2 text-sm"><code>SUMIF</code> הוא חד-תאי מטבעו. עטיפה ב-<code>ARRAYFORMULA</code> לא הופכת אותו למודע-מערך: עדיין מקבלים מספר אחד, לא עמודה לכל שורה. הצוות ראה את זה בשטח יותר מפעם אחת.</p>
  </ComparePanel>
  <ComparePanel label="עובד" tone="positive">
    <pre className="font-mono text-xs"><code>{`=ARRAYFORMULA(IF(B2:B13="Yoav", F2:F13, 0))`}</code></pre>
    <p className="mt-2 text-sm">השתמשו בתבנית ה-IF המודעת-למערך כדי לייצר תוצאה לכל שורה. אם רציתם בעצם סכום קבוצתי, הורידו את ה-ARRAYFORMULA ופשוט כתבו SUMIF.</p>
  </ComparePanel>
</Compare>

<Compare>
  <ComparePanel label="שבור" tone="negative">
    <pre className="font-mono text-xs"><code>=ARRAYFORMULA(F2:F-E2:E)</code></pre>
    <p className="mt-2 text-sm">טווחים פתוחים בלי guard לשורות ריקות. Sheets עושה spill של "0" לכל שורה ריקה עד שורה 1000+, מזהם sums שמתבססים עליו ומאט recalc בכל שינוי.</p>
  </ComparePanel>
  <ComparePanel label="עובד" tone="positive">
    <pre className="font-mono text-xs"><code>{`=ARRAYFORMULA(IF(E2:E="", "", F2:F-E2:E))`}</code></pre>
    <p className="mt-2 text-sm">טווח פתוח עם guard לשורות ריקות. ה-spill נעצר בשורת הנתונים האחרונה. התבנית לכל עמודה ב-"לוג שגדל".</p>
  </ComparePanel>
</Compare>

<Compare>
  <ComparePanel label="שבור" tone="negative">
    <pre className="font-mono text-xs"><code>{`=ARRAYFORMULA(IF(B2:B="Yoav", VLOOKUP(C2:C, X:Y, 2, FALSE), 0))`}</code></pre>
    <p className="mt-2 text-sm">לא <code>VLOOKUP</code> ולא <code>XLOOKUP</code> מטפלים באופן אמין ב-search key וקטורי בתוך <code>ARRAYFORMULA</code> ב-Sheets: מקבלים את ה-lookup של התא הראשון משוכפל מטה לכל שורה, לא תוצאה מודעת-לשורה. הבאג של "ערכים שגויים על פני כל העמודה", ואחת הסיבות הכי נפוצות שעמודת XLOOKUPs מחזירה ערך אחד משוכפל שתים-עשרה פעמים.</p>
  </ComparePanel>
  <ComparePanel label="עובד" tone="positive">
    <pre className="font-mono text-xs"><code>{`=ARRAYFORMULA(IF(B2:B="Yoav", INDEX(Y:Y, MATCH(C2:C, X:X, 0)), 0))`}</code></pre>
    <p className="mt-2 text-sm"><code>INDEX</code> + <code>MATCH</code> מטפלים נקי תחת <code>ARRAYFORMULA</code>: <code>MATCH</code> מחזיר מיקום אחד לכל שורה של search key, ו-<code>INDEX</code> קורא את הערך התואם מעמודת ההחזרה לכל אחת. כשצריך lookups פר-שורה ש-<code>INDEX/MATCH</code> לא יכול לטפל בהם (רב-קריטריונים, החזרה מותנית, כל דבר מתוחכם יותר), שלפו את <code>MAP</code> עם <code>LAMBDA</code> שעוטף את ה-lookup, מכוסה בשיעור 1.18.</p>
  </ComparePanel>
</Compare>

<QuickCheck
  question="לוג קמפיינים בן 12 שורות. כותבים =ARRAYFORMULA(F2:F-E2:E) ב-G2 (משתמשים בטווחים פתוחים F:F ו-E:E). מה קורה?"
  options={[
    {
      id: "a",
      label: "G2:G13 מתמלא ב-12 ערכי הרווח ועוצר.",
      correct: false,
      explanation: "טווחים פתוחים לא נעצרים בנתונים: הם מגיעים עד תחתית הגיליון (לרוב 1000+ שורות).",
    },
    {
      id: "b",
      label: "G2:G1000+ מתמלא ב-12 רווחים ואחריהם מאות אפסים (או #DIV/0!), מזהם sums שמתבססים עליו ומאט את הגיליון.",
      correct: true,
      explanation: "טווחים פתוחים עושים spill עד הסוף. תמיד תוסיפו guard לשורות ריקות: IF(E2:E=\"\", \"\", F2:F-E2:E).",
    },
    {
      id: "c",
      label: "G2 מתמלא רק עם רווח השורה הראשונה; הנוסחה לא עושה spill.",
      correct: false,
      explanation: "היא כן עושה spill: זה מה ש-ARRAYFORMULA עושה. הבעיה היא שהיא עושה spill הרבה רחוק יותר ממה שתכננתם.",
    },
  ]}
/>

## נפוץ בשטח

- **עמודות מחושבות בראש כל גיליון קמפיינים.** רווח, ROI, EPC, שולי רווח. כל אחת היא ARRAYFORMULA אחת בשורה 1 או 2 שממלאת את כל העמודה. קמפיינים חדשים מודבקים מתחת והעמודות המחושבות מתרחבות אוטומטית.
- **עמודות נירמול.** עמודת שם media buyer נקייה שנבנית מהעמודה הגולמית עם `ARRAYFORMULA(TRIM(LOWER(B2:B)))` כדי ש-lookups שמתבססים עליה לא ייכשלו על whitespace או case.
- **דגלי סטטוס.** תיוגי "Profitable", "Star", "Loss" בנוסחה אחת במקום אחת לשורה.
- **מפתחות cohort.** ARRAYFORMULA אחת שבונה "2026-05" מעמודת launch_date לקיבוץ מבוסס cohort.

## מלכודות מקצועיות

- **טווחים פתוחים (`E2:E`) בלי guard לשורות ריקות ממלאים את כל הגיליון.** מיליון שורות של "#DIV/0!" או "0" יאטו את הגיליון לזחילה. תמיד להגן עם `IF(E2:E="", "", ...)`.
- **ARRAYFORMULA יכול להחביא באגים בשורות בודדות.** אם שורה 50 שגויה, תקבלו שגיאה בתא של ה-spill אבל לא תוכלו לתקן אותה ישירות: רק על ידי עריכת הנוסחה הראשית.
- **IF בתוך ARRAYFORMULA מחשב את שני הענפים לכל שורה.** אם ענף אחד יקר (XLOOKUP, regex), הוא רץ גם כשלא נבחר. העלות מצטברת על נתונים גדולים.
- **חלק מהפונקציות לא עובדות בתוך ARRAYFORMULA.** אגרגטורים כמו SUM, AVERAGE, COUNT: עטיפה של טווח עם SUM בתוך ARRAYFORMULA מחזירה את הסה"כ, לא סכומים שורה-שורה. השתמשו ב-`SUMIFS` לכל שורה או שנו את המבנה.
- **אי אפשר לערוך תאי spill בנפרד.** אם הצוות רגיל לדרוס תא כדי לתקן ערך ידנית, ARRAYFORMULA שובר את התהליך הזה. תקשרו את השינוי.
- **טווחי spill יכולים להתנגש.** אם ב-G1 יש ARRAYFORMULA שעושה spill ל-G1:G12 ומישהו מקליד ב-G5, כל הנוסחה מחזירה שגיאה עם `#REF!`. הודעת השגיאה מצביעה על G1, אבל הסיבה היא היכן שיש את ההתנגשות.

## עכשיו לעבודה אמיתית

לחצו על **Start lesson** כדי להעלות את לוג הקמפיינים. תכתבו שלוש ARRAYFORMULAs: עמודת רווח עם טווח קבוע, עמודת רווח עם טווח פתוח ו-guard לשורות ריקות, ועמודת דגל סטטוס עם IF בתוך ARRAYFORMULA.
