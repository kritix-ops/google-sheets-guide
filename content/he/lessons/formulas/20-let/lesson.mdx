import { CAMPAIGNS } from "@/content/datasets/adtech";

# LET: שמות לערכי ביניים בתוך נוסחה

`LET` מאפשרת להקצות שמות לערכים בתוך נוסחה אחת. השמות קיימים רק בתוך ה-`LET`: הם לא על הגיליון, הם לא ב-namespace של החוברת, הם תחומים לתא היחיד. קוראים את הנוסחה ורואים את השמות; סוגרים את התא והם נעלמים.

הסיבה לכך כפולה. ראשית, נוסחאות מורכבות הופכות קריאות: במקום `=(F2-E2-G2*0.05)/F2` כותבים `LET(rev, F2, fee, G2*0.05, profit, rev - E2 - fee, profit / rev)` והקורא סורק שמות במקום לפענח חשבון. שנית, נוסחאות איטיות הופכות מהירות: כשאותו תת-ביטוי מופיע חמש פעמים, `LET` מאפשרת לחשב אותו פעם אחת ולהפנות אליו בשם. Sheets מעריך כל קישור פעם אחת, גם אם הגוף מפנה אליו פעמים רבות. בדאשבורדים שעמוסים ב-`IMPORTRANGE` זה ההבדל בין דאשבורד שמחושב מחדש בשנייה לבין אחד שלוקח 8 שניות.

## תחביר

```
LET(name1, value1, name2, value2, ..., body)
```

מחליפים זוגות של שם/ערך, ואז מסיימים עם הגוף: הביטוי שמשתמש בשמות. הגוף הוא הארגומנט האחרון הנדרש היחיד; כל מה שלפניו בא בזוגות.

<FunctionRef name="LET" />

קישור מאוחר יותר יכול להשתמש בקודם, וכך בונים pipelines. הגוף חייב להיות ביטוי אחד בדיוק (אין נקודות-פסיק, אין statements). אם צריך מבנה פנימי, מקננים בתוכו `LET` נוסף.

### דוגמה 1: נתוני צעצוע

חישוב ROI קטן. קושרים את `spend` ו-`revenue` לתאים, ואז כותבים את הגוף כחשבון קריא באנגלית.

<RefDemo
  data={[["Spend", "Revenue"], [100, 250]]}
  formula="=LET(spend, A2, revenue, B2, (revenue - spend) / spend)"
  formulaCell="D2"
  fillTo="D2"
  expectedFills={["=LET(spend, A2, revenue, B2, (revenue - spend) / spend)"]}
  caption="שני קישורים, גוף אחד. תוצאה: (250-100)/100 = 1.5. אותו דבר כמו =(B2-A2)/A2 אבל נקרא ככוונה עם שמות במקום כקואורדינטות של תאים."
/>

### דוגמה 2: נתוני אדטק אמיתיים

גרסת לוג הקמפיינים. אותה צורה, תאים אמיתיים, מאפשרת לקורא של דאשבורד לסרוק את הנוסחה בלי לפענח אותיות של עמודות.

<TryIt
  data={CAMPAIGNS}
  inputCell="H2"
  expectedFormula="=LET(spend,E2,revenue,F2,(revenue-spend)/spend)"
  task={<>חשבו ROI לקמפיין הראשון ב-<code>H2</code> באמצעות LET. קשרו <code>spend</code> ל-E2, <code>revenue</code> ל-F2, והגוף מחשב <code>(revenue - spend) / spend</code>.</>}
  hint={<>תבנית: <code>LET(name1, value1, name2, value2, body)</code>. הגוף הוא הארגומנט האחרון והוא פשוט ביטוי.</>}
  answer={<>לחישוב אחד זה overkill. היתרון הוא בנוסחאות עם 3+ שלבים, שבהן מתן שמות לערכי ביניים מסיר את הניחושים: קורא יכול לסרוק <code>profit, revenue - cost</code> במקום לפענח <code>F2-E2-G2*0.05</code>. ערך הביניים גם עוזר בדיבוג: אפשר לשנות רק קישור אחד כדי לבדוק וריאציות.</>}
/>

## למה LET חשוב: הימנעות מ-"מרק spreadsheet"

בלי `LET`, נוסחאות מורכבות פר-שורה הופכות למחרוזות מקוננות בלתי קריאות:

```
=IF((F2-E2)/E2>0.5, "Scale", IF((F2-E2)/E2>0, "Hold", IF((F2-E2)/E2>-0.2, "Watch", "Kill")))
```

אותו `(F2-E2)/E2` נכתב ארבע פעמים. Sheets מבצע אופטימיזציה לחישוב, אבל הקורא עדיין רואה את הרעש, ושינוי הגדרת ה-ROI (נגיד, חיסור של עמלה) משמעו לחפש את כל ארבעת העותקים.

עם `LET`:

```
=LET(
  roi, (F2 - E2) / E2,
  IF(roi > 0.5, "Scale",
   IF(roi > 0, "Hold",
   IF(roi > -0.2, "Watch", "Kill")))
)
```

`roi` מחושב פעם אחת, נמצא בשימוש ארבע פעמים, ומקבל שם. הוספת העמלה היא עריכה של תו אחד בקישור.

### דוגמה משולבת: שיוך ROI לקבוצה באמצעות LET

<TryIt
  data={CAMPAIGNS}
  inputCell="H2"
  expectedFormula='=LET(roi,(F2-E2)/E2,IF(roi>0.5,"Scale",IF(roi>0,"Hold",IF(roi>-0.2,"Watch","Kill"))))'
  task={<>שייכו את הקמפיין הראשון ל-Scale / Hold / Watch / Kill על בסיס ROI. הגדירו <code>roi</code> עם LET, ואז קננו IFs מולו.</>}
  hint={<>קישורים: <code>roi, (F2-E2)/E2</code>. הגוף הוא IF מקונן שמשתמש ב-<code>roi</code>. ספים: 0.5 / 0 / -0.2.</>}
  answer={<>ההכרעה לשורה 2 (Yoav, Car Deals PR): spend 342.18, revenue 487.30, ROI ≈ 0.424 → <code>Hold</code> (בין 0 ל-0.5). בלי LET אותה לוגיקה משתמשת ב-<code>(F2-E2)/E2</code> שלוש פעמים. עם LET, הוא מקבל שם פעם אחת והשאר נקרא כלוגיקת החלטה, לא כחשבון.</>}
/>

## LET נקשר בסדר: pipelines

קישור מאוחר יכול להשתמש בקודמים. ככה בונים שרשרות גזירה:

```
=LET(
  spend, SUM(E2:E13),
  revenue, SUM(F2:F13),
  profit, revenue - spend,
  roi, profit / spend,
  IF(roi > 0.3, "Strong period", "Weak period")
)
```

חמישה שלבים, כל אחד בעל שם, כל אחד שמיש שוב, כל אחד מחושב במונחי הקודמים. הקורא רואה את הלוגיקה מלמעלה למטה: השג spend, השג revenue, גזור profit, גזור ROI, החלט. אותה לוגיקה ללא `LET` היא ביטוי חשבון ענק אחד שאף אחד (כולל הכותב חצי שנה מאוחר יותר) לא רוצה לעשות לו debug.

## LET לביצועים

אם אותו תת-ביטוי יקר מופיע מספר פעמים, `LET` חוסכת חישוב מחדש. זה חשוב במיוחד כשהתת-ביטוי הוא `QUERY` איטי, `IMPORTRANGE`, או פעולת מערך גדולה.

```
=LET(
  data, QUERY(IMPORTRANGE("...", "raw!A:F"), "SELECT * WHERE Col2 = 'Yoav Cohen'", 1),
  rows, ROWS(data),
  total, SUM(QUERY(data, "SELECT Col6", 0)),
  rows & " campaigns, " & TEXT(total, "$#,##0") & " revenue"
)
```

`data` (ה-`IMPORTRANGE`+`QUERY` האיטי) מחושב פעם אחת. בלי `LET`, הייבוא רץ פעמיים (פעם ל-`ROWS`, פעם ל-`SUM`) ואתם משלמים את סבב הרשת שתי הפעמים. בדאשבורד שגוזר עשר מטריקות ממקור אחד, החיסכון מצטבר, ואתם נשארים מתחת למכסת התעבורה של `IMPORTRANGE` שאחרת הופכת את כל החוברת ל-`#REF!`-ים עד אמצע אחר הצהריים.

## מלכודות נפוצות

<Compare>
  <ComparePanel label="שבור" tone="negative">
    <pre className="font-mono text-xs"><code>{`=LET(net, revenue - spend, spend, E2, revenue, F2, net)`}</code></pre>
    <p className="mt-2 text-sm"><strong>הפניות קדימה.</strong> הקישור הראשון (<code>net</code>) מנסה להשתמש ב-<code>revenue</code> וב-<code>spend</code>, אבל הם מוגדרים מאוחר יותר. <code>LET</code> נקשרת בסדר; שם מוקדם לא יכול לראות מאוחרים. הנוסחה מחזירה שגיאה של שם לא פתור.</p>
  </ComparePanel>
  <ComparePanel label="עובד" tone="positive">
    <pre className="font-mono text-xs"><code>{`=LET(spend, E2, revenue, F2, net, revenue - spend, net)`}</code></pre>
    <p className="mt-2 text-sm"><strong>תלויות קודם.</strong> הגדירו <code>spend</code> ו-<code>revenue</code>, ואז גזרו את <code>net</code> מהם, ואז החזירו את <code>net</code>. הקישורים זורמים מלמעלה למטה בסדר גזירה.</p>
  </ComparePanel>
</Compare>

<Compare>
  <ComparePanel label="שבור" tone="negative">
    <pre className="font-mono text-xs"><code>=LET(SUM, 5, SUM + 1)</code></pre>
    <p className="mt-2 text-sm"><strong>הצללה של שם פונקציה מובנית.</strong> Sheets מסרב לקשור את <code>SUM</code> כשם LET כי הוא שם של פונקציה מובנית. הנוסחה מחזירה שגיאה כבר בזמן parse.</p>
  </ComparePanel>
  <ComparePanel label="עובד" tone="positive">
    <pre className="font-mono text-xs"><code>=LET(total, 5, total + 1)</code></pre>
    <p className="mt-2 text-sm"><strong>בחרו שמות שלא מתנגשים עם פונקציות מובנות.</strong> השתמשו בשמות תיאוריים (<code>total</code>, <code>spend_total</code>, <code>roi</code>) או שמות קצרים של אות אחת שברור שאינם מובנים.</p>
  </ComparePanel>
</Compare>

<QuickCheck
  question={"אתם כותבים =LET(data, IMPORTRANGE(url, \"A:F\"), filtered, FILTER(data, INDEX(data,,2)=\"Yoav\"), rows, ROWS(filtered), total, SUM(INDEX(filtered,,6)), rows & \" campaigns, $\" & total). מה היתרון בביצועים מול המקבילה ה-inline?"}
  options={[
    {
      id: "a",
      label: "גרסת ה-LET לא צריכה לעשות את ה-IMPORTRANGE בכלל.",
      correct: false,
      explanation: "היא עדיין עושה את ה-IMPORTRANGE, אבל פעם אחת. הגרסה ה-inline הייתה עושה אותו מספר פעמים (פעם אחת לכל הפנייה ל-data).",
    },
    {
      id: "b",
      label: "ה-IMPORTRANGE וה-FILTER רצים פעם אחת כל אחד. הגרסה ה-inline הייתה עושה את ה-IMPORTRANGE פעמיים (פעם ל-data של filtered, פעם ל-data של total) ואת ה-FILTER מספר פעמים. LET קושרת תת-ביטויים יקרים לשמות שמחושבים פעם אחת ומופנים פעמים רבות.",
      correct: true,
      explanation: "זאת התבנית הקנונית של IMPORTRANGE. LET הופכת N סבבי רשת לאחד ו-N מעברי FILTER לאחד. בדאשבורד עם 5+ מטריקות נגזרות מאותו מקור, החיסכון מצטבר.",
    },
    {
      id: "c",
      label: "גרסת ה-LET שומרת את התוצאה במטמון לשאר החוברת.",
      correct: false,
      explanation: "קישורי LET תחומים לתא. הם לא שומרים במטמון בין תאים. היתרון הוא בתוך נוסחה אחת.",
    },
  ]}
/>

## נפוץ בשטח

- **מחשבוני KPI** שמקבלים buyer + תקופה ומחזירים הכרעה. `LET(spend, ..., revenue, ..., roi, ..., verdict, IF(...))`.
- **תאי סיכום דאשבורד** שמחשבים שינויים תקופה-מול-תקופה. קושרים `current` ו-`previous`, ואז גוזרים `delta` ו-`pct_change`.
- **דאשבורדים עמוסי-`IMPORTRANGE`.** קושרים את הנתונים המיובאים פעם אחת בראש ה-`LET`; מפנים אליהם כמה פעמים שצריך בגוף. חותך את זמן התגובה בחצי על ייבואים איטיים (שיעור 1.27 מכסה את דפוסי IMPORTRANGE הרחבים יותר).
- **בתוך `LAMBDA`.** כל גוף `LAMBDA` לא-טריוויאלי משתמש ב-`LET` כדי לפרק את החישוב לשלבים בעלי שם. השילוב הוא מה שעושה את ה-named functions (שיעור 1.21) קריאות.
- **סיכומים מותנים.** קושרים תת-קבוצה מסוננת פעם אחת, ואז מחשבים כמה סטטיסטיקות עליה (count, sum, average) בלי לסנן מחדש.

## מלכודות מקצועיות

- **שמות לא יכולים להאפיל על פונקציות מובנות.** `LET(SUM, 5, SUM)` מחזיר שגיאה. בחרו שמות כמו `total`, `spend_total`, `s` במקום.
- **שמות חסרי רגישות לאותיות אבל מוצגים כמו שהוקלדו.** `LET(Roi, 0.5, ROI)` עובד אבל נקרא מוזר. בחרו קונבנציה של case (lowercase הכי נפוץ) ודבקו בה.
- **הגוף חייב להיות ביטוי אחד בדיוק.** אין נקודות-פסיק, אין statements. אם צריך מספר דברים, החזירו אותם במערך או קננו `LET` נוסף.
- **קישורים מוערכים בשקיקה.** `LET(x, slow_function(), 0)` עדיין משלם את עלות `slow_function()` למרות שהגוף לא משתמש ב-x. קשרו רק ערכים שתשתמשו בהם בפועל.
- **הפניות קדימה לא עובדות.** קישור יכול להשתמש בשמות מוקדמים יותר, אף פעם לא במאוחרים יותר. סדרו קישורים בסדר גזירה.
- **`LET` בתוך `LAMBDA` בסדר גמור ושימושית.** גוף `LAMBDA` חייב להיות ביטוי אחד. `LET` היא הדרך הסטנדרטית לתת לביטוי הזה מבנה פנימי.
- **אל תפריזו במתן שמות.** `LET` של 3 שורות עם 8 קישורים, חצי מהם בשימוש פעם אחת, קשה יותר לקריאה מהצורה ה-inline. השתמשו ב-`LET` כשהשמות מוסיפים בהירות, לא כי שמעתם שצריך להשתמש ב-`LET`.
- **`LET` לא שומרת במטמון בין תאים.** קישורים חיים להערכת נוסחה אחת בתא אחד. אם שני תאים חולקים את אותו תת-ביטוי יקר, העבירו אותו לתא עזר יחיד ששניהם מפנים אליו.

## עכשיו לעבודה אמיתית

לחצו על **Start lesson** כדי לפתוח את לוג הקמפיינים. תכתבו שלוש נוסחאות מבוססות-`LET` בעומק עולה: שינוי שם בסיסי של ROI, סיווג רב-שלבי (Scale/Hold/Watch/Kill), וסיכום ברמת תיק שמצרף spend ו-revenue ומפיק הכרעה במשפט אחד.
