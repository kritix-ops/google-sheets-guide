import { CAMPAIGNS_LARGE } from "@/content/datasets/adtech";

# מבנה של spreadsheet: ברור מההתחלה

Track 1 דיבר על נוסחאות. Track 2 מדבר על ה-spreadsheet. אותה נוסחה ב-spreadsheet מסודר היא כלי, ב-spreadsheet מבולגן היא חוב טכני שאף אחד אחר בצוות לא יכול לאסוף. העיקרון הראשון להפנים: **raw נשאר raw, הסיכום יושב בנפרד, והגבול ביניהם נראה במבט אחד**. מחיר הטעות פה צץ אחר כך: `SUMIFS` שבור, Pivot tables שבורים, ו-chart שכולל באופן מסתורי את שורת ה-"Total" בממוצע שלו.

spreadsheet שמציית לכלל הזה נקרא כמו אפליקציה קטנה. שורות חדשות מגיעות וה-dashboard מתעדכן. מישהו מצטרף לצוות ומוצא את הנתונים הקנוניים בגיליון ששמו מתחיל ב-`Raw -`. נוסחה נשברת ואפשר להתחקות אחריה במהלך קפיצה אחת או שתיים חזרה לבלוק ה-raw. spreadsheet שמתעלם מהכלל הזה הוא מבוך: סכומים מוטמעים ב-raw, raw מוטמע בסיכומים, כל dashboard הוא חד-פעמי שאף אחד אחר לא יכול לתחזק.

## המקור היחיד של האמת

ל-spreadsheet צריך להיות בלוק תאים אחד שהוא הנתונים הגולמיים הקנוניים, ושום דבר לא משנה אותו חוץ מה-pipeline שמעלה (הקלדה ידנית, IMPORTRANGE, פול של Apps Script). כל השאר, הסכומים, היחסים, ה-dashboards, ההתראות, יושב במקום אחר וקורא מהבלוק הגולמי.

הצורה של ה-spreadsheet האמיתי של הצוות בשטח נראית ככה:

| גיליון | מה יושב שם |
|---|---|
| `Raw - Taboola` | פול של IMPORTRANGE מ-spreadsheet ה-export של Taboola. לא נוגעים. |
| `Raw - Google` | פול של IMPORTRANGE מ-Google Ads export. לא נוגעים. |
| `Raw - Outbrain` | פול של IMPORTRANGE מ-Outbrain. לא נוגעים. |
| `Aggregator` | מאחד את גיליונות ה-raw ללוג מנורמל אחד דרך QUERY. פלט לקריאה בלבד. |
| `Dashboard` | כל ה-SUMIFS, ה-pivot של media buyer מול פלטפורמה, סיכום ה-ROI. קורא מ-`Aggregator`. |

הגבול בין "raw" ל-"derived" הוא גבול של גיליון, וגיליונות מקבלים שם עם prefix של `Raw -` כדי שכל מי שפותח את ה-spreadsheet יידע איזה משטח בטוח לגעת בו. הגיליונות עם `Raw -` נמלאים על ידי `IMPORTRANGE` (שיעור 1.27) מגיליון ה-export של כל פלטפורמה; הם נעולים לכתיבה לפי הקונבנציה כדי שעריכה מקרית ב-dashboard לא תרעיל את המקור.

<MiniGrid
  data={CAMPAIGNS_LARGE.slice(0, 6)}
  caption="ששת השורות הראשונות מתוך הלוג הגולמי של הצוות בן 60 שורות. A1:G61 הוא המלבן החסום; בלוק הסיכום יושב ב-I:J באותו גיליון, מופרד על ידי עמודה ריקה H."
/>

<QuickCheck
  question="חבר/ה לצוות פותח/ת את ה-spreadsheet ורואה ארבעה גיליונות: 'Campaigns', 'Aggregator', 'Dashboard', 'Notes'. איזה גיליון הוא ככל הנראה הנתונים הגולמיים הקנוניים בגיליון שנבנה לפי הקונבנציות של הצוות?"
  options={[
    {
      id: "a",
      label: <code>Dashboard</code>,
      correct: false,
      explanation: "Dashboard נמצא במורד הזרם. הוא קורא מ-Aggregator, אף פעם לא מחזיק נתונים גולמיים.",
    },
    {
      id: "b",
      label: <code>Aggregator</code>,
      correct: false,
      explanation: "Aggregator מאחד מספר גיליונות raw ללוג מנורמל אחד. זה פלט לקריאה בלבד, לא ה-raw הקנוני.",
    },
    {
      id: "c",
      label: <code>Campaigns</code>,
      correct: true,
      explanation: "אף אחד מהגיליונות לא משתמש ב-prefix הקונבנציונלי 'Raw -', אבל Campaigns הוא היחיד שמחזיק שורות של נתוני מקור. ב-workbook עם שמות תקינים הוא היה 'Raw - Campaigns'.",
    },
  ]}
/>

## לא לעשות אגרגציה בתוך הבלוק הגולמי

הטעות הכי שכיחה של מתחילים היא להוסיף שורת "Total" בתחתית הלוג הגולמי. זה נראה מועיל. זה שובר הכל במורד הזרם:

- **`SUMIFS` על העמודה סופר את שורת ה-Total פעמיים** אם תא ה-buyer של שורת ה-Total ריק והקריטריון שלך הוא `<>""`.
- **Pivot table שבנוי על הטווח יציג "Total" כשם של media buyer** ויכניס שורה רפאית לכל פירוט.
- **Charts דינמיים שמצביעים על הטווח החסום** מתרחבים בשקט לכלול את שורת ה-Total, גוררים את הממוצעים והטרנדים.
- **מיון של הטווח** משחיל את שורת ה-Total לאמצע הנתונים.

התיקון הוא לשים את הסכום *מחוץ* לבלוק הגולמי. או בראש אותו גיליון (אזור סיכום שהעין קוראת קודם), או בגיליון Summary נפרד. שניהם עובדים. הנקודה היא שהמלבן הגולמי נשאר מלבן של שורות גולמיות, בלי אגרגציה בתוכו.

<Compare>
  <ComparePanel label="שבור: Total בתוך ה-raw" tone="negative">
    <pre className="font-mono text-xs"><code>{`A          B           ... F        G
Date       Buyer            Spend    Revenue
2026-05-01 Yoav Cohen       342.18   487.30
...
2026-05-10 Ben Nahum        478.30   982.70
           TOTAL            3933.68  5694.10  ← בתוך המלבן`}</code></pre>
    <p className="mt-2 text-sm">Pivot table על <code>A1:G62</code> מציג "TOTAL" כ-buyer. ה-chart על Spend כולל את עמודת הטוטל, מגמד כל שורה אמיתית. מיון לפי Buyer משחיל את הטוטל לאלפבית.</p>
  </ComparePanel>
  <ComparePanel label="עובד: Total מחוץ ל-raw" tone="positive">
    <pre className="font-mono text-xs"><code>{`A          B           ... F        G   H   I              J
Date       Buyer            Spend    Rev      Total Spend    =SUM(F2:F61)
2026-05-01 Yoav Cohen       342.18   487.30   Total Revenue  =SUM(G2:G61)
...                                           Portfolio ROI  =(J2-J1)/J1
2026-05-10 Ben Nahum        478.30   982.70`}</code></pre>
    <p className="mt-2 text-sm">המלבן הגולמי נשאר <code>A1:G61</code>. בלוק הסיכום יושב ב-<code>J:J</code>, מופרד על ידי <code>H</code> ריק. Pivots, charts ומיונים על הטווח הגולמי נשארים נקיים.</p>
  </ComparePanel>
</Compare>

## הצורה של אזור הסיכום

כש-ה-spreadsheet קטן מספיק שגיליון Summary נפרד מרגיש כבד, מציבים את תאי הסיכום באותו גיליון, מופרדים מהבלוק הגולמי על ידי **עמודה ריקה אחת**. העמודה הריקה היא הרמז הוויזואלי שאומר "אזור אחר". כשבונים dashboards מעל, נוסחאות ה-dashboard מצביעות על הטווח החסום של המלבן הגולמי, ותאי הסיכום מצביעים על סכומים שכבר חישבת פעם אחת ורוצה להשתמש בהם שוב.

הצורה בתאים, כשהלוג הגולמי תופס את `A1:G61` והסיכום ב-`I:J`:

```
A         B          ...    G         H    I              J
Date      Buyer            Revenue        Total Spend    =SUM(F2:F61)
2026-04-06 Yoav Cohen      587.40         Total Revenue  =SUM(G2:G61)
2026-04-06 Dina Dayan      198.00         Portfolio ROI  =(J2-J1)/J1
...                                       Unique buyers  =COUNTA(UNIQUE(B2:B61))
```

שתי סיבות שהפריסה הזו שווה את המשמעת:

1. **הטווח הגולמי הוא מלבן `A1:G61` נקי.** כל Pivot table, chart, או `QUERY` קורא אותו בלי לוגיקה מותנית כדי לדלג על שורת Total.
2. **תאי הסיכום הם בלוק קבוע קטן.** כשכותבים את נוסחת ה-ROI, מפנים ל-`J1` ול-`J2`, לא ל-`SUM(F2:F61)` שוב. שינוי הגבול מ-`F61` ל-`F500` ורק שני תאי ה-`SUM` צריכים תיקון. תא ה-ROI הולך אחריהם אוטומטית.

<QuickCheck
  question="כותבים את ה-ROI של ה-portfolio כ-=SUM(G2:G61)/SUM(F2:F61)-1 ב-J3, וחוזרים על ה-SUM-ים שכבר קיימים ב-J1 וב-J2. למה זה פחות טוב מ-=J2/J1-1?"
  options={[
    {
      id: "a",
      label: "זה מחזיר מספר אחר",
      correct: false,
      explanation: "שתי הנוסחאות מחזירות אותו מספר. ההבדל הוא במחיר התחזוקה, לא בנכונות.",
    },
    {
      id: "b",
      label: "זה מכריח עריכה מחדש של שלושה תאים כשטווח הנתונים מתרחב משורה 61 לשורה 500, במקום שניים",
      correct: true,
      explanation: "Compute once, reference many. כל SUM כפול הוא מקום שתשכחו לעדכן כשהנתונים גדלים. J3 שקורא מ-J1 ו-J2 הוא תיקון אחד מרחק מכל שינוי טווח.",
    },
    {
      id: "c",
      label: "Sheets שומר את J2 ו-J1 ב-cache מהר יותר מ-SUM חדש",
      correct: false,
      explanation: "Sheets לא 'שומר ב-cache' reference לתא מהר יותר מ-SUM. הסיבה לשימוש חוזר היא תחזוקה, לא ביצועים.",
    },
  ]}
/>

## טווחים חסומים מנצחים עמודות מלאות

המשמעת השנייה שמשתלמת על פני כל נוסחה ב-spreadsheet: לכתוב טווחים שמסתיימים בשורת הנתונים האחרונה, לא בעמודה עצמה. `SUM(F2:F61)` חסום; `SUM(F:F)` לא חסום. שניהם מחזירים את אותו מספר היום. הם מתנהגים שונה לחלוטין ככל שה-spreadsheet גדל.

<Compare>
  <ComparePanel label="לא חסום: SUM(F:F)" tone="negative">
    <pre className="font-mono text-xs"><code>{`=SUM(F:F)
=AVERAGEIF(B:B, "Yoav Cohen", F:F)
=COUNTIF(D:D, "Taboola")`}</code></pre>
    <p className="mt-2 text-sm">נראה עמיד לעתיד: הוספת שורות "פשוט עובדת". אבל כל חישוב מחדש עובר על כל העמודה (Sheets מקצה מקום למאות אלפי תאים). ב-spreadsheet של 50,000 שורות עם 30 נוסחאות כאלה, זה הופך לעיכוב של שניות לכל עריכה. Track 4 שיעור 2 מכסה את העלות לעומק.</p>
  </ComparePanel>
  <ComparePanel label="חסום: SUM(F2:F61)" tone="positive">
    <pre className="font-mono text-xs"><code>{`=SUM(F2:F61)
=AVERAGEIF(B2:B61, "Yoav Cohen", F2:F61)
=COUNTIF(D2:D61, "Taboola")`}</code></pre>
    <p className="mt-2 text-sm">חסום לנתונים בפועל. חישוב מחדש נוגע רק ב-60 השורות שקיימות. כשהלוג גדל, מעדכנים את הגבול בשני תאי הסיכום (ה-<code>LET</code> משיעור 1.20 עם גבול נקוב הופך את זה לעריכה של שורה אחת). ה-dashboard נשאר מהיר.</p>
  </ComparePanel>
</Compare>

## שורת כותרת: שורה אחת, בלי merge

כותרות נראות תמימות והן הסיבה השנייה הכי גדולה לעבודה שבורה במורד הזרם, אחרי שורות Total.

- **שורת כותרת אחת.** לא שתיים. שורת כותרת כפולה מבלבלת כל `QUERY` שמנסה להשתמש ב-labels ושוברת את הסמנטיקה של `header=1`.
- **בלי תאים ממוזגים בכותרת.** מיזוג של "Q1 2026" וארבע תת-עמודות נראה מסודר בהדפסה, אבל `INDEX/MATCH` ו-`XLOOKUP` רואים רק את התא הראשון ו-`null` בשאר.
- **שמות שתואמים את התוכן של העמודה, לא את התצוגה שלה.** `Spend` (תואם למה שה-formula bar קורא) עדיף על `Spend ($)` (שמכריח כל נוסחה שמפנה אליו לעטוף את הסוגריים).

אם יורשים spreadsheet עם שורת כותרת ממוזגת, התיקון הזול הוא להוסיף שורה שנייה מוסתרת מתחת עם ה-labels הלא-ממוזגים ולגרום לכל נוסחה להפנות לשורה ההיא.

<Compare>
  <ComparePanel label="כותרת שבורה" tone="negative">
    <pre className="font-mono text-xs"><code>{`Row 1: |    Identity    |       Money        |
Row 2: | Date | Buyer  | Spend | Revenue | ROI |`}</code></pre>
    <p className="mt-2 text-sm">שתי שורות כותרת עם קבוצות "Identity" ו-"Money" ממוזגות. <code>QUERY</code> עם <code>header=1</code> קורא את שורה 1 (לרוב תאי null) בתור ה-labels של העמודות. כל הפניית עמודה נשברת.</p>
  </ComparePanel>
  <ComparePanel label="כותרת תקינה" tone="positive">
    <pre className="font-mono text-xs"><code>{`Row 1: | Date | Buyer | Spend | Revenue | ROI |`}</code></pre>
    <p className="mt-2 text-sm">שורה אחת, בלי merges, שמות שתואמים לתוכן. <code>QUERY</code>, <code>XLOOKUP</code> ו-<code>INDEX/MATCH</code> קוראים את הכותרות כצפוי.</p>
  </ComparePanel>
</Compare>

## עכשיו לעבודה אמיתית

<MiniGrid
  data={CAMPAIGNS_LARGE.slice(0, 6)}
  caption="הלוג הגולמי תופס את A1:G61. בלוק הסיכום שלכם יושב ב-I:J, מופרד על ידי עמודה ריקה H."
/>

לחצו על **Start lesson** כדי להעלות את לוג הקמפיינים בן 60 השורות. תמלאו בלוק סיכום של 4 תאים בעמודה J, עם labels שכבר נשתלו בעמודה I. הבלוק מתרגל שלושה עקרונות: טווחים חסומים (לא `F:F`), שימוש חוזר (J3 מפנה ל-J1 ול-J2 במקום לחשב את ה-SUM-ים שוב), וספירת `UNIQUE` של media buyers ייחודיים. כל תא לבד הוא שורה אחת, ביחד הם אזור סיכום אמיתי.

## נפוץ בשטח

ב-spreadsheets האמיתיים של הצוות:

- **כל גיליון `Raw -` הוא מלבן נקי.** בלי סיכומים, בלי הערות, בלי כותרות ממוזגות. הערות יושבות ב-cell notes, לא בתאים.
- **גיליון ה-Aggregator הוא לקריאה בלבד לפי הסכמה.** עריכה שלו אסורה כי הרענון הבא של IMPORTRANGE היה דורס את השינוי. הגדרות השיתוף בפועל אוכפות את זה לצוות.
- **בלוקי סיכום בפינה הימנית-עליונה של dashboards.** "Spend MTD", "Revenue MTD", "Portfolio ROI", "Active campaigns". שלושה עד שישה תאים מקסימום. הטבלאות הגדולות וה-charts באים מתחת.

## מלכודות מקצועיות

- **שורת ה-Total ששוברת `SUMIFS`.** חטא חוזר. אחרי שמסירים אותה, נוסחאות במורד הזרם פשוט עובדות.
- **אגרגציה עם `SUM(F:F)` במקום `SUM(F2:F61)`.** references לעמודה מלאה נראים עמידים לעתיד אבל מכריחים את Sheets לעבור על כל תא ריק בכל חישוב מחדש. ב-spreadsheet עם 50,000 שורות זה הופך לעיכוב של שניות לכל עריכה.
- **שם של גיליון לפי תאריך** (`2026-Q2`). כשהרבעון מתחלף, כל נוסחה שמפנה ל-`'2026-Q2'!B2` נשברת עד שמישהו משנה את השם. משתמשים בשמות יציבים (`Raw - Taboola`) ונותנים לתאריך לחיות בתוך עמודה.
- **שורות מוסתרות שמתחזות לנתונים שנמחקו.** Filter views בטוחים, אבל שורות שהוסתרו ידנית עדיין מסתכמות ב-`SUM` ונספרות ב-`COUNTA`. Pivot tables גם כוללים אותן. אם שורה צריכה להיות מחוצה, היא צריכה להיות מסוננת או מועברת לגיליון `Archived`.
