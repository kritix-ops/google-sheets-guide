import { CAMPAIGNS_LARGE, CAMPAIGNS } from "@/content/datasets/adtech";

# איך Gemini רואה spreadsheet

לפני שכותבים prompt טוב ל-Gemini ב-Sheets, צריך להבין מה הוא בעצם קורא כשלוחצים Enter. התשובה צרה יותר ממה שרובם מניחים, והיא משתנה לפי איזה גיליון פעיל, לאיזה טווח מצביעים, וכמה גדול ה-workbook. צריך להתייחס ל-Gemini כמו לחבר צוות שמסתכל לכם מעבר לכתף, עם חלון קריאה מצומצם. הוא רואה את מה שעל הגיליון הפעיל ועוד snapshot מובנה של התאים סביב ה-prompt. אין לו subscription חי לשאר ה-Drive.

זה המודל המנטלי שכל ה-track הזה תלוי בו. שיעורים 5.2 עד 5.6 מלמדים את פונקציית ה-AI, Fill with Gemini, ה-side panel של build and edit, ניתוח עם AI, ו-framework להחלטה מתי לא להשתמש ב-AI. כל אחד מהם מניח שאתם כבר יודעים מה Gemini באמת רואה ברגע שאתם קוראים לו. טעות בשיעור הזה והנוסחאות ירגישו רדופות: תוצאות שסותרות את הנתונים, סיכומים שמפספסים את המובן מאליו, סיווגים שנודדים משורה לשורה. הבנה נכונה הופכת את Gemini לאנליסט ג'וניור מהיר וממוקד.

## מה Gemini קורא אוטומטית

כשקוראים ל-`=AI()` מתא או פותחים את ה-side panel, חלון ה-context של Gemini שולף snapshot של הגיליון הפעיל. אין שלב setup; לא מפרטים אילו עמודות רלוונטיות. המודל פשוט מסתכל על מה שיש לכם, בערך בסדר העדיפויות הבא: הטווח שהעברתם כארגומנט שני, התאים סביב הנוסחה, ואז הגיליון הרחב יותר.

תוכן ה-snapshot מתחלק לשש קטגוריות, ובכל אחת מהן יש trade-off בגיליון adtech אמיתי.

| קטגוריה | מה בטווח | Trade-off |
|---|---|---|
| ערכי תאים | הערכים שהייתם רואים על המסך, בדגימה או בתקציר כשהמידע גדול | דגימה אומרת שתשובות לגיליונות גדולים הן קירובים, לא ספירות מדויקות |
| נוסחאות | טקסט הנוסחה, לא רק הערך המחושב | Gemini קורא `=SUMIFS(...)`, וזה נהדר; הוא גם יכול לקרוא נוסחה מורכבת בטעות ולהסביר את הדבר הלא נכון בביטחון |
| מבנה הגיליון | שורות כותרת, סוגי נתונים, שמות גיליונות, named ranges | אם הכותרות שלכם דו-משמעיות (`Buyer` מול `Media Buyer`), Gemini מנחש, לרוב לא נכון |
| עריכות אחרונות | כמה השינויים האחרונים שעשיתם | שימושי ל-prompts מסוג "תקן את מה שזה עתה שברתי"; לא תחליף ל-version history |
| Charts ו-Pivots | בגיליון הפעיל, כולל טווחי המקור | אם טווח המקור של הגרף שבור, Gemini עדיין מתאר את הגרף כאילו הוא תקין |
| Cell notes ו-comments | הערות גלויות בגיליון הפעיל | PII שנשכח ב-comment נמצא ב-snapshot |

כדי לפתוח את ה-side panel: לוחצים על הכפתור **Ask Gemini** בפינה הימנית-עליונה של ה-spreadsheet (אייקון הניצוץ ליד כפתור ה-Share), או בקיצור `Ctrl+Alt+G` ב-Windows / `Cmd+Ctrl+G` ב-Mac. הפאנל מתעגן בצד ימין ונשאר פתוח תוך כדי עבודה, מעוגן לטאב הפעיל.

<FunctionRef name="AI" />

מילת המפתח בטבלה היא *הגיליון הפעיל*. גיליונות אחרים באותו spreadsheet עשויים להיות בדגימה דלילה או מדולגים לחלוטין, תלוי ב-prompt ובגודל ה-workbook. Spreadsheets אחרים ב-Drive שלכם לא נמצאים בטווח, אלא אם מדביקים את הנתונים, או, ב-Workspace plans חדשים יותר, ה-side panel מושך אותם במפורש דרך Workspace Intelligence. שיעורים 5.4 ו-5.5 נשענים על ההבחנה הזו.

<QuickCheck
  question="כתבתם =AI('Summarize this dataset') בתא בטאב 'Daily' אבל השמטתם את ארגומנט הטווח. התשובה של Gemini מדברת על הטאב 'Campaigns' במקום. מה הסיבה הסבירה ביותר?"
  options={[
    {
      id: "a",
      label: <>Gemini הוזה (hallucinates), והתשובה אמורה להיזרק.</>,
      correct: false,
      explanation: "אפשרי, אבל ההסבר הספציפי יותר הוא שבלי טווח, Gemini עושה snapshot של הסביבה של הגיליון הפעיל. אם הסתכלתם על Campaigns לפני, או החלפתם טאב באמצע הקריאה, זה מה שנחת ב-context.",
    },
    {
      id: "b",
      label: <>בלי טווח מפורש, Gemini מתעגן על התאים סביב הנוסחה ועל הסביבה של הגיליון הפעיל; זה מה שעיצב את התשובה.</>,
      correct: true,
      explanation: "נכון. תמיד מעבירים טווח חסום כמו A2:F31 כשחשוב לכם איזה מידע Gemini קורא. ארגומנט הטווח הוא מה שמקבע את ה-snapshot.",
    },
    {
      id: "c",
      label: <>פונקציית ה-=AI() תמיד קוראת את כל הטאבים ב-workbook.</>,
      correct: false,
      explanation: "לא נכון. AI() ממוקדת בגיליון הפעיל בתוספת מה שמעבירים כארגומנט שני. טאבים אחרים מחוץ לטווח אלא אם מפנים אליהם במפורש.",
    },
  ]}
/>

## חיתוך: הבעיה של הגיליונות הגדולים

לוג קמפיינים של 60 שורות נכנס ב-snapshot אחד. Aggregator של 50,000 שורות לא נכנס. כשהגיליון הפעיל גדול מדי לחלון ה-context, Gemini רואה דגימה, לא את כל הנתונים. הדגימה היא תערובת כלשהי של הראש, הזנב, ואמצע מרובד. אין שליטה באילו שורות.

הצוות מרגיש את זה בכל פעם שמישהו מבקש מ-Gemini לסכם את ה-aggregator הראשי. התשובה נשמעת בטוחה; היא מצטטת media buyers ספציפיים ומספרים ספציפיים; המספרים ברובם נכונים. ב-5% מהמקרים שהם לא, אף אחד לא שם לב כי המשפט קורא נקי.

שתי השלכות תפעוליות:

1. **סיכומים של גיליונות גדולים הם קירובים.** "איזה media buyer הוציא הכי הרבה בחודש שעבר" יכול להיות לא נכון אם השורות הרלוונטיות לא היו בדגימה.
2. **ספירות וסכומים מ-prompts מילוליים לא אמינים.** משתמשים ב-`SUM`, ב-`COUNTIF`, או ב-`QUERY` למספרים. משתמשים ב-prompts של AI לעבודה איכותית כמו סיווג וסיכום.

התיקון בשטח: אגרגציה מקדימה. מריצים `QUERY` (שיעור 1.12) שמגלגל את 50,000 השורות ל-row-per-buyer או ל-row-per-day, מכניסים את התוצאה לגיליון, ושולחים את ה-prompt על הגיליון המגולגל. אותו prompt מקבל תשובה הרבה יותר מדויקת כשהנתונים באמת נכנסים.

<Compare>
  <ComparePanel label="שבור" tone="negative">
    <pre className="font-mono text-xs"><code>{`=AI("Which buyer had the highest total spend in April?", Aggregator!A:H)`}</code></pre>
    <p className="mt-2 text-sm">50,000 שורות, טווח לא חסום, prompt מילולי שמבקש מספר. Gemini רואה דגימה, בוחר media buyer שנראה הוצאתי בדגימה, וכותב משפט בטוח. התשובה האמיתית יכולה להיות שגויה.</p>
  </ComparePanel>
  <ComparePanel label="עובד" tone="positive">
    <pre className="font-mono text-xs"><code>{`=QUERY(Aggregator!A1:H50001,
  "SELECT B, SUM(F) WHERE A >= date '2026-04-01'
   AND A < date '2026-05-01' GROUP BY B
   ORDER BY SUM(F) DESC LIMIT 1")`}</code></pre>
    <p className="mt-2 text-sm">דטרמיניסטי, מדויק, חינם, מחזיר את אותה התשובה בכל refresh. AI אף פעם לא היה הכלי הנכון לשאלה הזו.</p>
  </ComparePanel>
</Compare>

## מה Gemini לא רואה

כמה קטגוריות שהצוות נופל בהן כל הזמן. צריך להעלות אותן לראש בכל פעם שכותבים prompt, וזה יחסוך לכם יום ברבעון בדיבוג.

- **מקורות IMPORTRANGE בין-spreadsheets.** Gemini רואה את הערכים *המופקים* שנחתו בגיליון, לא את ה-spreadsheet המקור. אם ה-aggregator שלכם מושך נתוני Taboola דרך `IMPORTRANGE`, Gemini רואה את השורות המיובאות. הוא לא יודע אם למקור היו עוד שורות שלא יובאו, אם טווח המקור חסום צר מדי, או אם ל-spreadsheet המקור יש טאבים אחרים. שיעור 1.27 ו-Track 4 שיעור 3 מכסים IMPORTRANGE לעומק.
- **טאבים שלא נפתחו לאחרונה.** טאבים באותו spreadsheet שלא נגעו בהם עשויים שלא להיות ב-context הפעיל לקריאת `=AI()` שיושבת בטאב אחר. אם רוצים ניתוח חוצה-טאבים, או נכנסים לטאב המקור קודם, או מעבירים את הטווח במפורש.
- **הרשאות ושיתוף.** Gemini לא יודע מי יש לו גישה לקובץ. לא שואלים אותו.
- **שורות מוסתרות שסוננו.** Filter views מסתירים שורות מהעין, והדגימה של Gemini עשויה להתנהג לפי אותם כללי גלויות. שורות שהוסתרו ידנית הן אזור אפור; בודקים לפני שסומכים.
- **מידע מחוץ ל-Sheets.** אין תוכן Drive, אין thread-ים של Gmail, אין אירועי Calendar אלא אם ה-Workspace Intelligence בצד ה-side panel מופעל ב-plan שלכם. אל תניחו שהוא מופעל. שואלים את ה-admin.

<MiniGrid
  data={CAMPAIGNS.slice(0, 7)}
  caption="נתונים קטנים: 6 שורות קמפיינים. קטנים מספיק ש-Gemini רואה כל תא, כל נוסחה, כל header. זה הגודל היחיד שבו אפשר לסמוך על prompts כמו 'הצג את שלושת הראשונים לפי revenue' בלי וידוא."
/>

### מלכודת קלה: ה-tab הפעיל היה הלא נכון

הבאג הכי מדווח של "Gemini נתן לי תשובה מוזרה" בצוות יש לו אותו שורש: tab אחר היה פעיל כש-prompt נשלח. גם prompts ב-side panel וגם קריאות `=AI()` מתעגנים על ה-tab שמוצג ברגע השליחה. החלפת tab *אחרי* שכתבתם את ה-prompt לא משנה בדיעבד את מה ש-Gemini ראה.

<Compare>
  <ComparePanel label="שבור (tab drift)" tone="negative">
    <pre className="font-mono text-xs"><code>{`1. אתם עובדים על ה-Aggregator tab.
2. אתם לוחצים על Daily Summary tab כדי
   להציץ במספר.
3. אתם חוזרים ל-side panel ולוחצים Enter על
   "Summarize this dataset".
4. Gemini מסכם את Daily Summary, לא את Aggregator.

הסיכום מזכיר 30 שורות ומגמה יומית נקייה.
ל-Aggregator יש 50,000 שורות והוא הנושא בפועל.
התשובה נקראת בסדר; התשובה היא על ה-tab הלא נכון.`}</code></pre>
    <p className="mt-2 text-sm">ה-tab הפעיל בזמן השליחה מנצח. הכוונה שבראש שלכם מפסידה. אין שום אזהרה.</p>
  </ComparePanel>
  <ComparePanel label="עובד (טווח מפורש)" tone="positive">
    <pre className="font-mono text-xs"><code>{`=AI("Summarize this dataset", Aggregator!A1:H50001)`}</code></pre>
    <p className="mt-2 text-sm">ארגומנט הטווח מקבע את הסנפשוט בלי קשר ל-tab הפעיל. תמיד מעבירים טווח חסום כשהתשובה תלויה באילו נתונים Gemini קורא.</p>
  </ComparePanel>
</Compare>

## אי-דטרמיניזם: אותו prompt, שתי תשובות

זו התכונה של AI ששוברת הכי הרבה אינטואיציות מ-spreadsheet. כל פונקציה אחרת בגיליון היא פונקציה טהורה: אותם קלטים, אותו פלט, בכל פעם. `=SUM(F2:F61)` תמיד מחזיר את אותו מספר. `=AI("Categorize: " & C2)` לא.

שתי קריאות של אותה נוסחת `AI()` עם אותם קלטים יכולות להחזיר שתי מחרוזות שונות. המודל לא דטרמיניסטי בעיצוב. מריצים את הנוסחה ביום שני, מישהו לוחץ **Refresh and insert** על תא ביום שישי, והפלט של אותו תא עשוי לזוז. הטריגר הוא תמיד אדם שלוחץ, אף פעם לא אירוע פסיבי: טעינה רגילה של הגיליון, חבר צוות שפותח את הקובץ, recalc אחרי שינוי upstream. שום אחד מאלה לא מריץ מחדש את `=AI()`. ההשלכה עדיין אמיתית: דוחות שאמורים להתאזן יחלקו אם מישהו הריץ מחדש את התא, והסיבה בלתי נראית כי התאים נראים כמו כל תא אחר.

<Compare>
  <ComparePanel label="טעינה רגילה: שום דבר לא משתנה" tone="negative">
    <pre className="font-mono text-xs"><code>{`H2: =AI("Risk: low/med/high. Spend " & F2 & " Rev " & G2)
... נגרר ל-H61, הפלט קפוא מהריצה של יום שני

שלישי: אתם טוענים מחדש את הגיליון.
רביעי: חבר צוות פותח אותו ועורך שורה 12.
חמישי: IMPORTRANGE upstream מתרענן.

H2:H61 לא משתנה. =AI() לא מחשב מחדש אוטומטית.`}</code></pre>
    <p className="mt-2 text-sm">אירוע פסיבי (טעינה מחדש, פתיחה, עריכה upstream, recalc) לא מייצר מחדש תא `=AI()`. הפלט סטטי עד שמישהו מריץ אותו מחדש בכוונה.</p>
  </ComparePanel>
  <ComparePanel label="Refresh and insert: הפלט עשוי לזוז" tone="positive">
    <pre className="font-mono text-xs"><code>{`שישי: חבר צוות לוחץ קליק ימני על H7 ולוחץ
"Refresh and insert" על תא ה-=AI().

הפלט של H7 משתנה מ-"medium" ל-"high".
ה-"high risk count" בדשבורד היה 7 אתמול, 9 היום.
הטריגר הוא אדם, לא refresh.`}</code></pre>
    <p className="mt-2 text-sm">רק לחיצה מכוונת על **Refresh and insert** (או ה-**Generate and insert** הראשוני) מריצה את המודל מחדש. שום דבר ב-workbook לא אומר לכם אילו תאים זזו או מי לחץ.</p>
  </ComparePanel>
</Compare>

המשמעת שנובעת מזה היא paste-as-values: מריצים `=AI(...)` ב-H2:H61, בודקים את העמודה, מעתיקים את H2:H61, ועושים paste-special > values ל-H2:H61. עכשיו H הוא טקסט קפוא, הדשבורד reproducible, ואפילו לחיצה מכוונת על Refresh and insert לא יכולה לשנות אותו בלי עריכת תא גלויה. מריצים מחדש שבועית אם רוצים סיווג טרי; מקפיאים בכל ריצה.

## עלות ומכסות

כל קריאה ל-`=AI()` וכל prompt ב-side panel מחויבים לפי ה-Workspace plan שלכם. אין "tier חינמי" בתוך מוצר Sheets; מודל החיוב זהה ל-Gemini בשאר ה-Workspace. כמה Workspace plans כוללים חלונות גישה פרומו עם מכסות פר-משתמש גבוהות יותר במהלך השקת Gemini-in-Sheets. בודקים מול ה-admin שלכם באיזה tier הצוות נמצא ואיזה מגבלות חלות; ה-gating זז ככל ש-features מגיעים ל-GA.

המלכודת שצריך לשים אליה לב: לגרור נוסחת `=AI()` למטה לאורך עמודה של 5,000 או 50,000 שורות. זה 5,000 או 50,000 קריאות בתשלום, וכל אחת איטית (שניות, לא אלפיות), אז החישוב מחדש והחשבון מצטברים מהר. התקרה על ריצות רב-תאיות היא 350 תאים ב-generate, אבל אפשר לחזור על הריצה, וזה מצטבר בעלות. משתמשים ב-AI ביד קלה בגיליונות גדולים. משתמשים בנוסחאות איפה שאפשר. שיעור 5.6 בונה את framework ההחלטה המלא.

<QuickCheck
  question="אתם עומדים לגרור =AI('Classify this vertical: ' & C2) למטה ב-10,000 שורות. איזה חשש הכי חשוב?"
  options={[
    {
      id: "a",
      label: <>החישוב מחדש יהיה איטי כי כל קריאה לוקחת שניות.</>,
      correct: false,
      explanation: "נכון, אבל זו הבעיה השנייה במידרג. הבעיה הגדולה יותר למטה.",
    },
    {
      id: "b",
      label: <>כל קריאה מחויבת, ו-10,000 קריאות בתשלום ל-refresh מצטברות מהר.</>,
      correct: false,
      explanation: "גם נכון, ובעיית עלות אמיתית. אבל יש חשש שלישי שלרוב מנצח את שני אלה.",
    },
    {
      id: "c",
      label: <>כל מה שלמעלה, ובנוסף שיעור שגיאה שקטה של ~5% על 10,000 תאים אומר כ-500 סיווגים שגויים שאף אחד לא תופס ב-review.</>,
      correct: true,
      explanation: "שלושתם מצטברים. איטי, יקר, ושקט-שגוי הם כל אחד רע בנפרד; ביחד הם הכלל 'אל תגרור AI() לאורך עמודה ארוכה'. אגרגציה מקדימה או paste-as-values.",
    },
  ]}
/>

## עכשיו לעבודה אמיתית

<MiniGrid
  data={CAMPAIGNS_LARGE.slice(0, 6)}
  caption="לוג הקמפיינים יושב ב-A1:G61. ה-scratchpad שלכם ב-H1:H3 עם labels בעמודה I."
/>

לחצו על **Start lesson** כדי להעלות את לוג הקמפיינים בן 60 השורות לטאב Campaigns. ממלאים שלושה תאים בעמודה H:

- **H1** הוא קריאת `=AI()` שמסכמת את הנתונים במשפט אחד, עם הטווח החסום `A1:G61` כארגומנט השני.
- **H2** הוא תזכורת ספרותית שמפרטת לפחות שלושה דברים ש-Gemini רואה בגיליון הפעיל (values, formulas, structure, edits, charts, named ranges).
- **H3** הוא תזכורת ספרותית על עלות ומכסות, מזכירה Workspace ו-plan.

שלושת התאים ביחד הם המודל המנטלי: קריאה לדוגמה, מה בטווח, מה זה עולה.

## נפוץ בשטח

ב-spreadsheet הצוותי האמיתי של ה-aggregator:

- **תא סיכום מוצמד יושב ב-A1 של כל dashboard.** פסקה אחת בעברית פשוטה שמתארת למה הגיליון הזה. Gemini קורא אותה כדבר הראשון ב-snapshot ומתעגן עליה, מה שהופך prompts ב-side panel להרבה יותר מקורקעים.
- **ל-aggregator של 50,000 שורות לעולם אין `=AI()` ישיר.** במקום זה, טאב `Daily Summary` מגלגל אותו ל-30 שורות. כל עבודת AI רצה על הסיכום, לא על הלוג הגולמי.
- **שמירות עלות יושבות בטאב נפרד.** השימוש במכסה מתועד ידנית פעם בשבוע כדי שהצוות ישים לב לקפיצה לפני שהחשבון מגיע.
- **דפוס "freeze with paste-as-values" הוא ה-audit trail.** כל עמודה שנוצרה על-ידי AI שמזינה דשבורד מוקפאת לפני שהדשבורד קורא ממנה. עמודת ה-AI החיה אף פעם לא מגיעה לדוח של בעלי עניין.

## מלכודות מקצועיות

- **הנחה ש-Gemini יודע לקרוא את המקור של IMPORTRANGE.** הוא לא. הוא רואה את הערכים המופקים שנחתו בגיליון. אם טווח המקור חתוך, Gemini רואה את הייבוא החתוך, לא את המקור המלא.
- **שתי קריאות AI ברצף מחזירות דברים שונים.** התצוגה של Gemini על הגיליון משתנה בין קריאות אם עורכים משהו. גם בלי עריכות, המודל אינו דטרמיניסטי. אסור להסתמך על כך שקריאה N+1 תשחזר את קריאה N.
- **PII ב-snapshot.** הגיליון הפעיל הוא מה ש-Gemini קורא. אם יש לכם email-ים של לקוחות, שמות, או כל PII אחר גלויים, הנתונים האלה ב-context. בודקים את תנאי הטיפול בנתונים של ה-Workspace plan לפני ששולחים PII דרך Gemini. שיעור 5.6 מרחיב את מדיניות ה-PII.
- **`=AI()` נגרר למטה לאורך עמודה של 50,000 שורות.** קריאות AI פר-שורה מצטברות מהר. תגיעו למכסה, תעלו את זמן החישוב, ותגלו ש-5% מהקריאות החזירו תשובות שגויות בעדינות שאף אחד לא תפס. אגרגציה מקדימה.
- **Charts ו-Pivots בטאבים מוסתרים.** הם יכולים להיות או לא להיות בטווח. כלל אצבע: אם ה-side panel אומר "Working with X tab", זה מה ש-Gemini קורא. אם רוצים שיקרא יותר, מחליפים טאב קודם.
- **Schema mismatch בין prompt לכותרות.** כותבים "Categorize this Profit value" אבל הכותרת היא `Net Revenue`. Gemini מנחש שיש עמודת Profit שלא קיימת, מחשב מספר סביר-נשמע, והדשבורד שלכם בשקט קורא ממנו. תמיד שמות שדה בטקסט הכותרת המדויק.
