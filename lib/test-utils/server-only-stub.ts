// Empty stub used by vitest to satisfy `import "server-only"` in modules
// that are tested in the Node test env. The real `server-only` package
// only exports under the `react-server` condition, which vitest's Node
// runtime does not match. See vitest.config.ts.
export {};
